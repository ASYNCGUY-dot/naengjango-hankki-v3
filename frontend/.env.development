# 로컬 개발은 이 저장소의 백엔드를 본다(배포된 V2가 아니라).
#
# 이유가 둘이다. 첫째, 배포된 V2에는 V3에서 추가한 CORS 설정이 없어서 브라우저가
# 모든 요청을 preflight에서 막는다. 둘째, V3에서 바꾼 인증·엔드포인트가 그쪽에는 없다.
#
# 백엔드 띄우기: .venv/Scripts/uvicorn.exe api.main:app --port 8001
VITE_API_BASE=http://localhost:8001
