# 냉장고 한끼 V3 — 프로젝트 보고서

작성일: 2026-08-18 (2026-08-19 개정)
기간: 2026-08-12 ~ 2026-08-19 (8일, 커밋 44개)
결과: **개발 완료, 배포 완료. 남은 것은 지인 5명 사용자 테스트 하나.**

배포 주소: https://naengjango-hankki-v3-web.onrender.com
저장소: https://github.com/ASYNCGUY-dot/naengjango-hankki-v3

---

## 1. V3를 한 이유

V2는 28일간 기능 84개를 완성하고 배포까지 마쳤다. 그런데 **실사용자가 0명이었다.**
가입 43명 중 냉장고에 재료를 넣은 사람이 3명, 후기 1개였고 이름 있는 계정은 전부
검증용이거나 개발자 본인 것이었다.

그래서 V3의 목표는 기능을 더 만드는 게 아니었다. 네 가지였다.

1. React 전환 — Reflex의 제약(프론트 테스트 도구 부재로 4,047줄 커버리지 0%,
   websocket 상시 프로세스, 상태 락 만료로 인한 로그인 실패)에서 벗어나기
2. 백엔드 정비 — 느린 추천, 빈 테스트, 죽은 코드
3. 회원·로그인 개편 — 실사용자가 생기기 전이 유일한 적기
4. 디자인 개편 — **종료 조건이 있는 기준으로**

원래 다섯 번째로 "런칭"이 있었으나 고정비를 이유로 범위에서 뺐다. 그 결정이 V3 전체의
성격을 정했다. **무료 티어 안에서 끝내는 재구축**이 됐고, 유료 전환이 유일한 해법인
항목들(콜드스타트 제거, 모니터링, DB 자동 백업)은 함께 미뤄졌다.

---

## 2. 만든 것

| 항목 | 규모 |
|---|---|
| 백엔드 | Python 48개 파일 7,767줄, 라우터 18개, 엔드포인트 51경로 |
| 프론트 | TypeScript 30개 파일 7,292줄, 화면 9개 |
| 테스트 | pytest 261개(3,707줄) + Vitest 155개(2,557줄) |
| 마이그레이션 | 6개 (001~007, 002는 파이썬 스크립트) |
| 데이터 | 레시피 1,148개, 식품영양 302,629건 (V2에서 그대로 재사용) |

**화면 9개**: 로그인·회원가입 / 홈 / 냉장고 / 추천 / 레시피 상세 / 마이 / 온보딩 /
비밀번호 초기화 / 약관·개인정보.

### V2 대비 가장 큰 구조 변화

**진짜 URL 라우팅이 생겼다.** V2는 화면 전체가 단일 페이지(`/demo`)였고 탭 전환을
State로만 처리했다. 그래서 레시피 상세를 링크로 공유할 수 없었고 뒤로가기도 동작하지
않았다. V3에서 `/recipe/67` 같은 주소로 쪼갰고, **로그인 없이도 열린다.**

---

## 3. 기술 판단 세 가지

### 3.1 Next.js가 아니라 Vite + React를 골랐다

착수 계획의 추천은 Next.js였다. SSR이 있으면 레시피 상세가 검색엔진에 잡히기 때문이다.
그런데 뒤집었다. 결정적인 근거는 **SSR이 백엔드 콜드스타트 동안 오히려 해롭다**는
것이었다. 서버가 렌더링하려면 API를 기다려야 하는데 그 API가 15분 유휴 후 잠든다.
사용자는 그동안 빈 화면만 본다.

정적 SPA는 셸이 즉시 뜨고 그 위에 "서버 깨우는 중"을 띄울 수 있다. 실측이 이를
뒷받침한다 — 정적 사이트 0.29초, API 콜드스타트 22.9초.

### 3.2 API 응답 타입을 손으로 쓰지 않는다

`scripts/dump_openapi.py`가 저장소 코드에서 OpenAPI 명세를 뽑고 `openapi-typescript`가
프론트 타입을 만든다. CI가 둘의 어긋남을 검사하므로 백엔드 응답 모양이 바뀌면 프론트
빌드가 먼저 깨진다.

명세를 **배포된 서버가 아니라 저장소 코드에서** 뽑는 것이 핵심이다. 처음에는 배포된
V2에서 받았는데 그 명세에 V3에서 이미 지운 엔드포인트가 남아 있었다. 타입으로 불일치를
잡겠다면서 정작 지금 코드가 아닌 것을 기준으로 삼은 셈이었다.

### 3.3 추천 성능 — 목표는 못 채웠고, 왜 못 채웠는지는 안다

착수 계획은 "웜 응답 2.6초 → 1초 미만"이었고 병목으로 IN절을 지목했다. **틀렸다.**

SQL과 파이썬을 같은 실행에서 갈라 재보니 **파이썬 75% / DB 25%**였고, 서버측 쿼리는
5개 합쳐 73.7ms였다. 진짜 원인은 점수 계산의 부분 문자열 검사가 추천 한 번에
**2,071만 번** 돌던 것이었다. 순수 함수 세 개에 `lru_cache`를 걸어 로컬 0.674초 →
0.20초로 줄였다.

그런데 Render 실측은 **2.4초**다. 무료 티어가 0.1 CPU라 CPU 바운드 구간만 증폭되기
때문이다(같은 서버에서 DB 바운드인 검색은 0.61초). 더 줄이려면 알고리즘을 손대야
하는데, 지인 테스트 전에 할 일이 아니라고 판단했다. 로딩 표시가 있으면 2.4초는 견딜
만하고, 진짜 문제는 첫 접속이다.

**교훈**: `EXPLAIN ANALYZE`만 봤다면 이 원인을 못 찾았다. 파이썬 몫이 거기 안 잡힌다.

---

## 4. 측정이 계획을 뒤집은 사례

이 프로젝트에서 반복된 패턴이다. **화면을 만들기 전에 실제 데이터를 확인하면 시안이
자주 틀렸다.**

| 계획한 것 | 데이터가 말한 것 | 결과 |
|---|---|---|
| 홈에 "인기 레시피" | 좋아요가 전체 4개, 전부 개발자 본인 | "레시피 둘러보기"로 교체 |
| 알레르기 정확 일치 필터 | `달걀`(200)과 `계란`(27)이 다른 태그 | 동의어 확장 추가 |
| 목록에서 상세를 따로 조회 | 카드 20개면 요청 20번 | 목록 API에 `image_url` 추가 |
| 홈 테마에 한식·중식·일식 | 요리 종류 컬럼이 없음 | 제철·재료수·칼로리로 대체 |
| 난이도별 분류 | 난이도 컬럼이 없음 | 재료 개수(7개 이하)로 대용 |

마지막 둘은 사용자 요청이었는데, 지어내는 대신 **왜 못 만드는지 설명하고 데이터가
지탱하는 대안을 제시**했다. "가지 탕수육"을 중식으로 넣을지가 한 번 어긋나면 사용자가
먼저 알아챈다.

### 4.1 전수 검토에서 나온 것 (2026-08-19)

지인 테스트 직전에 파일 전수 검토를 했다. 처음에는 grep 위주로 훑고 "고칠 것 없음"에
가까운 결론을 냈는데, 그건 검토가 아니었다. 다시 하니 **화면이 서버의 판정 기준을
따로 들고 있는 자리**가 네 군데 나왔고 전부 조용히 어긋나 있었다.

가장 컸던 것은 **재료가 화면에서 사라지던 버그**다. 화면이 "수량이 없으면 구획 제목"
이라고 판단했는데, 세어 보니 수량 없는 행 649개 중 진짜 제목은 4개뿐이었다. 나머지는
`소금  적당량`처럼 수량이 숫자가 아닌 글자로 적힌 **재료**다. 레시피 1,148개 중
**309개(27%)**가 영향을 받았고, 그 행이 목록 맨 뒤에 오면 그룹째 버려져 소금·후추가
화면에서 통째로 없어졌다.

같은 오판정이 세 곳을 동시에 망가뜨리고 있었다. 부족한 재료 목록이 "주재료가
부족합니다"라고 말했고, **추천 정렬 기준까지 틀어져 있었다** — 순위를 매기는
`missing_count`가 있지도 않은 재료를 세고 있었다. 화면 한 곳의 표시 문제로 보였던 것이
사실은 추천 품질 문제였다.

| 어긋난 곳 | 증상 | 고친 방식 |
|---|---|---|
| 재료/구획 판정 | 레시피 27%에서 재료 누락, 추천 순서 왜곡 | `portion_agent.classify_ingredient_row()` 하나로 통일 |
| 가입 연령대 | `60대 이상`을 고르면 영양 분석이 안 되는데 저장은 성공 | `GET /profile/options`가 실제 기준표에서 생성 |
| 병력 자유 입력 | "혈압 높음"은 고혈압 조정에 안 걸림 | 서버가 아는 다섯 개만 선택지로 |
| 표시용/정렬용 개수 | 카드는 7개, 상세는 5개 | 정렬은 조미료 제외, 표시는 포함으로 분리 명시 |

**어긋나도 아무 오류가 안 났다는 점이 핵심이다.** 테스트도 전부 통과했다 — 양쪽이 각자의
규칙 안에서는 옳았기 때문이다. 이 종류는 화면을 열고 원본 데이터를 세어 봐야만 보인다.

같은 검토에서 **서버에는 있는데 화면이 안 부르던 기능**도 이었다. 목표 대비 영양
적합도, 부족한 재료와 대체안, 재료 자동완성, 레시피 저장이다. 만들어 두고 연결을 잊은
것과, 안 만들기로 정한 것은 다르다 — 후자는 §9에 남겼다.

---

## 5. 배포에서만 드러난 문제들

로컬 테스트 416개가 전부 통과하는 상태에서도 배포하면 깨지는 것들이 있었다.

**정적 사이트 빌드가 실패했다.** `tsc`와 `vite`가 둘 다 devDependencies인데
`NODE_ENV=production`인 환경의 `npm ci`는 devDependencies를 건너뛴다. 배포 전에 같은
환경을 로컬에서 재현해 잡았다.

**API가 V2 저장소를 가리키고 있었다.** 블루프린트가 이름으로 서비스를 찾아 연결하면서
브랜치만 맞추고 저장소는 안 바꿨다. 몇 번을 재배포해도 8월 12일 코드가 올라왔다.

**비밀번호 초기화 메일이 안 나갔다.** Render 무료 웹 서비스가 SMTP 포트(25/465/587)를
차단한다. 자격증명 문제가 아니었다 — 같은 코드가 로컬에서는 2.5초에 발송됐다.
서버 로그의 요청 시간이 21초(= SMTP 타임아웃 20초)인 것이 단서였다.

**그 실패가 로그에도 안 남았다.** 라우터가 발송 결과를 그냥 버리고 있었다. 초기화 요청은
계정 존재 여부를 숨기려고 성공·실패에 같은 응답을 주므로, 사용자도 운영자도 알 방법이
없는 상태였다. 지금은 응답은 그대로 두되 서버 로그에는 남긴다.

**CORS는 배포 경계에서만 검증된다.** 로컬 프론트 → 로컬 백엔드로는 절대 안 잡힌다.
그래서 `scripts/smoke_test_deploy.py`를 만들어 배포된 조합을 실제로 태운다.

---

## 6. 안전과 개인정보

이 앱은 **알레르기 정보를 받아 그 재료가 든 레시피를 걸러낸다.** 그래서 조용한 실패가
가장 위험하다. 사용자는 걸러졌다고 믿는데 실제로는 아무것도 안 걸러지는 상태다.

세 곳에서 그 상태가 실제로 존재했거나 존재할 뻔했다.

1. **온보딩 화면이 없어 `users.allergy`가 계속 `NULL`이었다.** 필터가 한 번도 작동한
   적이 없었다. 화면을 만들어 해결했다.
2. **자유 입력이 태그와 안 맞았다.** 기존 데이터에 `콩`(태그는 `대두`), `@$#$` 같은
   값이 있었다. 서버가 실제 태그에서 만든 목록에서만 고르게 바꿨다.
3. **동의 없이 저장하면 조용히 비워질 뻔했다.** 건강 정보 동의를 넣으면서, 동의 없이
   온 알레르기는 비워서 저장하지 않고 **422로 거절**하도록 했다.

개인정보 쪽에서도 구멍을 찾았다. **동의 체크박스는 있는데 동의할 문서가 없었다.**
`user_consents` 테이블이 버전까지 기록하고 있었지만 그 버전이 가리키는 문서가
어디에도 없었다. 없는 문서에 대한 동의는 기록으로서 의미가 없다.

`/terms`와 `/privacy`를 만들고, 알레르기·병력에 대해서는 가입 때의 포괄 동의와 분리해
**그 값을 실제로 입력하는 온보딩에서 따로** 받는다. "요청하면 지웁니다"를 지킬 수 있도록
`scripts/delete_account.py`도 만들었다 — 약속만 하고 수단이 없으면 그 문서는 거짓말이 된다.

---

## 7. 품질 관리 방식

**변이 테스트를 습관으로 썼다.** 보안·안전에 걸린 코드는 테스트를 쓴 뒤 그 코드를
일부러 망가뜨려 테스트가 실제로 죽는지 확인했다. 관리자 권한 확인을 무력화하니 10개가
실패했고, 알레르기 동의어 확장을 끄니 2개, 건강 정보 게이트를 끄니 2개가 죽었다.
**죽지 않는 테스트는 통과해도 아무 말도 하지 않는다.**

**CI가 세 축을 본다.** 파이썬 문법·정적 검사, 프론트 테스트·타입·빌드, 그리고 진짜
Postgres를 띄워 마이그레이션 체인을 빈 DB에서 재생한다. 마지막 것이 없으면 sqlite로만
검증된 SQL이 운영에 처음 실행된다.

**배포 스모크 테스트**가 24항목을 실제 배포 환경에서 확인한다. 검증 계정을 스스로 만들고
지우므로 언제 돌려도 결과가 같고 운영 데이터를 더럽히지 않는다.

---

## 8. 실측치

| 항목 | 값 | 조건 |
|---|---:|---|
| 정적 사이트 첫 로드 | 0.29초 | CDN, 스핀다운 없음 |
| API 콜드스타트 | 22.9초 / 33.0초 | 유휴 후 2회. **회차 편차가 크다** |
| 추천 (웜) | 2.4초 | 5회 중앙값 |
| 레시피 검색 (웜) | 0.61초 | |
| 홈 테마 4줄 | 122ms | 로컬, 127.0.0.1 |
| Lighthouse 접근성 | **100점** | 모바일 |

콜드스타트를 두 번 제대로 쟀는데 22.9초와 33.0초로 갈렸다. 값 하나를 "그 서비스의
콜드스타트"라고 적으면 안 되는 종류의 수치다. 지인에게 보낼 안내에 30초라고 쓴 것은
그래서다 — 빨리 뜨면 아무도 불평하지 않지만 예상보다 늦으면 고장으로 읽힌다.

그 두 번 앞에 **틀린 측정이 두 번 더 있었다.** 측정 창 한가운데서 `main`에
push해 자동 배포가 서비스를 재시작시켰다. 두 번 다 1.5초로 "일관되게" 나온 것이 오히려
함정이었다 — 재배포 직후의 웜 응답이라 값이 안정적이었던 것이고, **일관성은 정확성의
근거가 아니다.**

로컬 측정도 한 번 크게 틀렸다. 모든 요청이 2초씩 걸려 새 엔드포인트가 느린 줄 알았는데,
DB를 안 쓰는 `/openapi.json`도 2038ms였고 `127.0.0.1`로 부르니 4ms였다. Windows에서
`localhost`가 IPv6로 먼저 풀리는데 uvicorn은 IPv4에만 바인딩돼 매 요청이 재시도를
기다리고 있었다.

---

## 9. 남은 것과 알려진 제약

### 남은 것

**지인 5명 사용자 테스트 하나다.** 준비물은 `docs/PHASE4_지인테스트.md`에 있다 —
보낼 메시지, 성능 실측치, 끝나고 볼 분석 쿼리 세 개(운영 DB에서 실행 확인함).
개발 중 검증 흔적은 지웠으므로 `usage_events`에 남은 것은 전부 실제 사용 기록이다.

이미 한 명이 있다. 링크를 뿌리기 전인 8월 18일에 가입해 재료 8개를 넣고 추천을 13번
받은 사람이 있다. **V2에서 43명 중 3명만 하던 그 행동이다.** 표본 하나로 결론을 낼 수는
없지만, 집계할 때는 `users.created_at`으로 Phase 4 참가자와 갈라 본다.

### 알려진 제약

**비밀번호 초기화 메일이 안 나간다.** 플랫폼 제약이다. 화면이 이 사실을 그대로 말하고,
`scripts/make_reset_link.py`로 링크를 직접 만들어 전달한다. 되살리려면 유료 인스턴스로
올리거나 HTTPS 메일 API로 바꾸면 되고, `mail_agent.send_mail()` 내부만 바꾸면 된다.

**추천이 웜 2.4초다.** 위 3.3 참고.

**이름이 같은 다른 레시피가 7쌍 있다.** 공공데이터에 같은 이름의 다른 조리법이 들어
있다. 합치면 정보가 사라지므로 두었다. 화면 낭독기로는 구분되지 않는다.

**엔드포인트 51개 중 화면이 부르는 것은 17개다.** 가격·쇼핑·안전정보·좋아요·후기·
사용자 레시피·제철·인기영상·관리자 기능이 서버에만 있다. **의도적이다** — 무엇이 쓸모
있는지 아직 아무도 모르는 상태에서 화면을 늘리면 V2의 실수를 반복한다. 지인 테스트에서
요청이 나온 것부터 잇고, 안 나온 것은 지운다.

---

## 10. 이 프로젝트에서 배운 것

**측정 없이 한 진단은 절반이 틀렸다.** 추천 병목(IN절 → 실제로는 파이썬), 메일 실패
원인(자격증명 → 실제로는 플랫폼), 콜드스타트(1.5초 → 실제로는 22.9초), 로컬 지연
(코드 → 실제로는 IPv6). 네 번 다 "그럴듯한 설명"이 먼저 떠올랐고 네 번 다 틀렸다.

**조용한 실패가 시끄러운 실패보다 나쁘다.** 알레르기 필터가 한 번도 안 돌았던 것,
초기화 메일이 로그도 없이 실패한 것, 동의 문서가 없는데 동의를 받고 있던 것, 레시피
27%에서 재료가 사라지던 것. 넷 다 아무도 불평하지 않는 상태로 존재했고 테스트도 전부
통과했다. 시끄럽게 깨지는 버그는 제보라도 받는다.

**같은 판정 기준을 두 곳에 두면 반드시 어긋난다.** 알레르기 선택지에서 한 번 겪고
"서버가 만들어 준다"로 고쳤는데, 그 교훈을 연령대·병력·재료 판정에 옮기지 않아 같은
사고를 세 번 더 냈다. 한 번 배운 원칙을 **비슷한 자리 전부에 적용했는지 확인하는 것**이
그 원칙을 배우는 것보다 어려웠다.

**검토는 파일을 열어야 검토다.** 개선점을 찾아달라는 요청에 처음에는 grep으로 훑고
답을 냈고, "대충 한 것 같다"는 지적을 받고 다시 했다. 두 번째에 나온 것이 위의 27%
버그와 어긋난 선택지들이다. 첫 번째 방식으로는 **하나도 못 찾았다.**

**push하고 CI를 안 보면 push 안 한 것과 같다.** 프론트 테스트 픽스처에 새 필드를 빠뜨려
CI가 두 번 빨갛게 떴는데, 그동안 다음 작업을 하고 있었다. 로컬에서 `tsc --noEmit`은
테스트 파일을 안 보고 `npm run build`와 CI만 본다 — **내가 안 돌리는 검사가 잡는
것들이 있으니 CI를 만든 것인데, 결과를 안 보면 그 자체가 무의미하다.**

**끝나지 않는 작업에는 종료 조건을 붙여야 한다.** "완벽한 디자인"은 끝이 없어서
다섯 개의 확인 가능한 조건으로 바꿨고, 그래서 실제로 끝났다(Lighthouse 100점 포함).

**V2의 진짜 실패는 기능 부족이 아니라 검증 부재였다.** 84개를 만들고 한 명에게도 확인
받지 않았다. V3는 화면 9개로 줄이고 대신 사용 로그를 심어 **어디서 이탈하는지 볼 수
있게** 만들었다. 그 데이터가 나오기 전까지는 기능을 더하지 않는다.
