[pytest]
# tests/에서 "from api.routers import ..." 같은 절대 import가 되려면 저장소 루트가
# sys.path에 있어야 한다. `python -m pytest`는 알아서 CWD를 넣어주지만, 그냥 `pytest`
# 명령으로 실행하면(CI에서 쓰는 방식) 안 넣어줘서 ModuleNotFoundError: No module named 'api'가
# 났다 - pythonpath로 명시해서 두 실행 방식 모두 똑같이 동작하게 한다.
pythonpath = .
