# 냉장고 한끼 V3 — 핸드오프 문서

작성일: 2026-08-12 (최종 갱신: 2026-08-18)
작성 맥락: V2를 `v2.0.0`으로 종료하고 V3를 새 폴더에서 시작하기로 함

> **이 문서를 읽는 Claude에게**
> 이 파일은 V3의 설계 판단과 실제로 겪은 함정을 담은 기록이다. 이전 세션의 대화
> 맥락은 없다고 가정하고 썼다.
>
> **지금 상태: Phase 1~3이 끝났고 배포까지 마쳤다.** 남은 것은 Phase 4(지인 5명
> 테스트)와 7절 종료 조건 4번(Lighthouse)뿐이다. 무엇부터 볼지는 이렇다.
>
> - 코드를 만지기 전에 **8절(함정)을 먼저 읽을 것.** 전부 실제로 당한 것들이고,
>   특히 `8.-3`~`8.0`은 배포하면서 새로 배운 것이라 로컬 테스트로는 안 잡힌다
> - 지인 테스트를 진행한다면 `docs/PHASE4_지인테스트.md`에 보낼 메시지와 분석
>   쿼리가 있다
> - 6절의 "Phase 1에서 남은 것"과 10절은 **이미 끝난 작업의 기록**이다. 지시가
>   아니라 이력으로 읽을 것
>
> V2 저장소는 아래 경로에 그대로 남아 있다. 필요하면 직접 열어서 확인할 것.
> `C:\Users\Donga\Desktop\DONGA AI LAB\DONGA KDT 2ND PRO CHOI_persnal V2\냉장고한끼-v2`

---

## 1. 프로젝트가 무엇인가

**냉장고 한끼** — 냉장고에 있는 재료를 입력하면 공공데이터 기반 레시피 1,148개 중에서
그 재료를 가장 잘 활용하는 메뉴를 추천하는 모바일 우선 웹앱이다.

추천 외에 재료 안전정보 확인, 가격 비교, 제철 재료, 사용자 레시피 등록·승인, 후기,
즐겨찾기 기능이 있다. 데이터는 식약처·농림수산식품교육문화정보원 공공데이터와
YouTube API에서 온다.

V1 → V2 → V3로 이어지는 세 번째 버전이다.

---

## 2. V2는 무엇이었고 왜 끝냈나

V2는 **Reflex**(Python으로 React를 생성하는 프레임워크) 프론트 + 별도 FastAPI
백엔드 구조였다. 28일간 커밋 67개, 코드 12,166줄, 엔드포인트 53개, 기능 84개를
완성하고 배포까지 마쳤다.

**기능은 다 됐지만 실사용자가 0명이다.** 가입 43명 중 냉장고에 재료를 넣은 사람은
3명, 후기 1개뿐이고 이름 있는 계정은 전부 검증용이거나 개발자 본인 것이다.

V3로 넘어가는 이유는 네 가지다.

1. V2 종합 보고서에서 나온 문제점·개선사항 해결
2. **React 전환**
3. 실사용 테스트 전 디자인 개편
4. 회원관리·로그인 데이터 개편

원래 다섯 번째로 "런칭"이 있었으나 **고정비가 발생해 V3 범위에서 제외**했다
(4절 참고).

### Reflex를 떠나는 구체적 근거

문서로 확인된 제약이 다섯 가지다. 프론트 테스트 도구 부재로 4,047줄 커버리지 0%,
websocket 상시 프로세스 강제, 상태 락 만료로 인한 로그인 실패, 그로 인한 호스팅
3분할, 로컬 개발 중 websocket 연결 끊김 반복.

**애초에 백엔드를 분리해둔 것도 React 전환을 대비해서였다.** 그래서 전환 비용이
낮다.

### 결정적 스코프 사실

**V2 프론트 전체가 단일 페이지다.** 화면 컴포넌트 함수 81개, 이벤트 핸들러 67개가
있지만 탭 전환을 State로만 처리하고 실제 라우트는 `/demo` 하나뿐이다.

즉 React로 가면 **진짜 URL 라우팅이 새로 생긴다.** 레시피 상세가 링크로 공유되고,
뒤로가기가 동작하고, 검색엔진에 잡힌다. 지금은 셋 다 안 된다.

---

## 3. 지금 존재하는 것

### 배포된 서비스 (2026-08-18 기준, 전부 무료 티어)

| 구성 | 주소 | 상태 |
|---|---|---|
| **V3 프론트 (Render 정적)** | `https://naengjango-hankki-v3-web.onrender.com` | 살아 있음. 0.29초, 스핀다운 없음 |
| **V3 API (Render 웹)** | `https://naengjango-hankki-v2-api.onrender.com` | 살아 있음. **V3 코드** |
| DB | Supabase Postgres | 그대로 |
| V2 프론트 (Vercel) | `https://naengjango-hankki-demo.vercel.app` | 아직 200으로 응답 |
| Reflex 백엔드 (Render) | `naengjango-hankki-v2-reflex.onrender.com` | **죽음**(연결 안 됨) |

**API 서비스 이름이 `v2-api`인 것은 오타가 아니다.** 살아 있는 Render 서비스와 이름이
묶여 있어 일부러 그대로 뒀다. 바꾸면 새 서비스가 생기고 주소가 끊긴다. 지금 이 서비스는
V3 저장소의 `main`을 보고 있다.

배포에서 실제로 물린 것들은 8.-3 ~ 8.0에 정리했다. 특히 이 서비스는 **처음에 V2 저장소를
가리키고 있었다** — 블루프린트가 이름으로 서비스를 찾아 연결하면서 브랜치만 `main`으로
맞추고 저장소는 안 바꿨다. 몇 번을 재배포해도 옛 코드가 올라와서 한참 헤맸다.

무료 티어라 API는 15분 유휴 후 중단된다. 정적 사이트는 CDN이라 해당 없다.

### 데이터 (Supabase, 그대로 재사용)

| 테이블 | 행 수 |
|---|---:|
| `ingredient_catalog` | 302,629 |
| `recipe_tags` | 16,315 |
| `recipe_ingredients` | 12,965 |
| `recipes` | 1,148 (98%에 유튜브 링크 있음) |
| `popular_videos` | 25 |
| `users` | **1** (005 적용 후 관리자 한 행. 적용 전에는 46개였다) |

이 데이터가 이 프로젝트에서 가장 대체하기 어려운 자산이다. 다시 만들려면 상당한
시간이 든다.

---

## 4. V3 범위

### 하는 것

- **React 전환** — V2 프론트를 React로 재작성, 진짜 라우팅 도입
- **백엔드 정비** — 추천 성능, 테스트 보강, 죽은 코드 정리
- **회원·로그인 개편** — 토큰 재설계, 계정 데이터 정리
- **디자인 개편** — 종료 조건이 있는 기준으로 (7절)
- **지인 5명 테스트** — 비용 0, V2에서 못 한 유일한 검증

### 하지 않는 것 (의도적 제외)

**런칭.** 사용자가 비용을 이유로 제외했다. 런칭하려면 Render Starter 서비스당
월 $7(콜드스타트 제거용)이 필요하고, 수익화를 붙이면 Vercel Hobby가 비상업적
용도로 제한되므로 Pro 월 $20이 추가된다. **V3는 무료 티어 안에서 끝낸다.**

런칭이 빠지면서 함께 미루는 것들이다.

- 콜드스타트 36초 대응 (유료 전환이 유일한 정공법)
- Sentry 에러 모니터링, DB 자동 백업
- 공공데이터 이용약관 개별 확인 (수익화 시 필수)
- 제휴 수익 연동 (쿠팡파트너스는 승인 + Deeplink API 필요)

**개인정보 처리방침 — 처리함 (2026-08-18).**

원래 이 항목은 "런칭 안 하면 법적 의무는 약하니 간단한 안내 문구 수준"이라고 적혀
있었다. 다시 보니 그렇지 않았다. 동의를 *받는* 쪽(가입 체크박스, `user_consents` 이력)은
만들어져 있는데 **동의 대상 문서가 존재하지 않았다.** 체크박스는 라벨뿐이고 링크도
문서도 없었으며, `CONSENT_VERSION`이 가리키는 문서가 어디에도 없었다. 없는 문서에 대한
동의는 기록으로서 의미가 없다.

셋을 만들었다.

- **`/terms`, `/privacy` 화면** — 실제 코드와 DB를 보고 썼다. 특히 없는 기능을 약속하지
  않았다. 기존 가입 화면 문구의 "회원 탈퇴 시 지웁니다"는 탈퇴 기능이 없으므로 거짓이었다
- **건강 정보 별도 동의** — 알레르기·병력은 가입 때의 포괄 동의로 덮지 않고, 그 값을
  실제로 넣는 온보딩에서 따로 받는다. 서버가 강제한다(`api/routers/profile.py`) — 동의
  없이 온 건강 정보는 저장하지 않고 422로 거절한다. 조용히 비워서 저장하면 사용자는
  알레르기를 넣었다고 믿는데 필터는 아무것도 안 거르는, 이 프로젝트에서 가장 위험한
  상태가 되기 때문이다. 건강 정보를 안 넣는 사람에게는 묻지 않는다
- **`scripts/delete_account.py`** — "요청하면 지웁니다"를 실제로 지킬 수단. 약속만 하고
  수단이 없으면 그 문서는 거짓말이 된다

**남은 확인 사항**: 한국 개인정보보호법상 알레르기·병력이 민감정보에 해당하는지, 해당한다면
지금의 별도 동의 방식이 요건을 만족하는지는 법률 검토가 필요하다. 이 문서들은 사실을
정확히 적은 안내문이지 법률 자문이 아니다.

**새 기능.** V3는 재구축이지 확장이 아니다. V2에 이미 엔드포인트 53개와 화면
81개분이 있는데 그중 무엇이 쓸모 있는지 아직 아무도 모른다. **지인 테스트에서
데이터가 나오기 전까지 기능을 추가하지 않는다.**

**추천 알고리즘 개정.** V2에서 9차까지 고쳤지만 전부 개발자 판단이었다. 사용
데이터가 나온 뒤에 손대는 게 맞다.

---

## 5. 결정된 것 (원래는 미결정 항목이었다)

세 가지 모두 2026-08-12에 사용자와 확인해 정했다. 추천안과 다르게 간 것이 하나 있으므로
근거를 남겨둔다.

**프레임워크 — Vite + React(SPA)로 갔다.** 이 문서의 원래 추천은 Next.js였는데
뒤집혔다. 결정적인 근거는 사용자가 짚은 것이다. **SSR은 백엔드 콜드스타트 동안 오히려
해롭다.** 서버가 렌더링하려면 API를 기다려야 하는데 그 API가 30초씩 자고 있으면 사용자는
빈 화면만 본다. 정적 SPA는 셸이 즉시 뜨고 그 위에 "서버 깨우는 중"을 띄울 수 있다.
실측이 이를 뒷받침한다 — 정적 사이트 0.29초, API 콜드스타트 30초대다. SEO를 포기하는
대가는 지인 5명 테스트 단계에서 치를 게 없다.

**백엔드는 새 폴더에 복사했다.** 복사할 것과 안 할 것은 9절에 정리했다.

**PWA는 보류했다.** Phase 3까지 오면서 다시 꺼내지 않았고, 지금도 없다.

---

## 6. 단계 계획

**순서에 중요한 점이 하나 있다. 디자인을 React 착수 전에 확정한다.** 컴포넌트를
만들고 나서 디자인을 바꾸면 같은 것을 두 번 만들게 된다.

### Phase 1 — 백엔드 정비 + 인증 개편

React가 소비할 API를 먼저 안정시킨다. 느리고 검증 안 된 API 위에 프론트를 올리면
우회 코드를 심게 되고 그게 그대로 V3의 부채가 된다.

- ~~**추천 웜 응답 2.6초 → 1초 미만.**~~ **처리함 (2026-08-12, 커밋 `1fcedac`).**
  이 항목의 원래 서술은 틀렸다. 병목은 `get_candidate_recipes()`의 IN절이 아니라
  `score_by_ingredients()`의 파이썬 부분 문자열 검사였다. 실측 근거는 아래와 같다.
  - SQL과 파이썬을 같은 실행에서 갈라 재니 **파이썬 75% / DB 25%**였다. SQL만 보면
    파이썬 몫이 안 잡히므로, `EXPLAIN ANALYZE`만으로는 이 원인을 못 찾는다
  - 서버측 쿼리 시간은 5개 합쳐 실행 49.3ms + 계획수립 24.5ms = **73.7ms**다.
    로컬에서 크게 보이던 DB 시간은 대부분 Supabase까지의 네트워크였다
  - 프로파일에서 `_belongs_to_group()`의 비교가 추천 한 번에 **2,071만 번** 돌았다.
    같은 재료명이 후보 1,144개에 걸쳐 반복 유입되는데 매번 다시 계산한 탓이다
  - 순수 함수 3개(`_group_membership`, `is_staple`, `ingredients_match`)에
    `lru_cache`를 걸어 해소했다. 로컬 0.674초 → 0.20초, 파이썬만 0.509초 → 0.074초
  - **Render 수치는 아직 미확정이다.** 무료 티어가 0.1 CPU라 CPU 바운드 구간만
    증폭되며(같은 서버에서 검색 0.41초 / 추천 2.6초였던 이유), 추정 0.5초 안팎이지만
    배포해서 재기 전까지는 계산일 뿐이다
  - 남은 여지: 파라미터 1,144개짜리 IN절을 `= ANY(배열)`로 바꾸면 계획수립 24.5ms가
    줄어든다. 몫이 작아 우선순위는 낮다
- ~~**회원·로그인 개편.**~~ **처리함 (커밋 `913b64d`, `49e8c39`).** 토큰에 30일
  만료와 슬라이딩 갱신을 넣었고, `expires_at`을 해석할 수 없는 토큰은 거부한다.
  작업 중 발견한 것 둘이 원래 계획보다 컸다.
  - `auth_tokens`가 `001_schema.sql`에 없었다. V2에서 운영 DB에 직접 만들어져
    외래키·인덱스·NOT NULL이 전부 없는 상태였다. 004가 셋을 함께 채운다
  - `username`에 UNIQUE 제약이 없었다. `signup()`이 "확인 후 INSERT"라 경합에
    취약했다. DB 제약으로 막고 위반을 409로 바꿔 답한다
  - `POST /profile`이 인증 없이 users 행을 만들던 것이 빈 계정 34개의 출처였다.
    제거하고 인증된 `PUT /profile/{user_id}`로 일원화했다
- ~~**admin 라우터 테스트.**~~ **처리함 (커밋 `4146413`).** 29개를 추가했다.
  상태 코드만 보지 않고 거부 뒤 DB 상태가 그대로인지 함께 본다 — reject는 레시피를
  완전히 삭제하므로 특히 그렇다. 변이 테스트로 실제 검출력을 확인했다
  (`_require_admin` 무력화 시 10개 실패, `require_self` 무력화 시 6개 실패)
- ~~**CI에 Postgres 서비스 컨테이너 추가.**~~ **처리함 (커밋 `468e191`).**
  `postgres-check` 잡이 마이그레이션 체인 재생과 어댑터 동작을 검증한다.
  준비 과정에서 체인이 빈 DB에서 재생되지 않는 구멍 둘을 찾아 고쳤다.
  **아직 한 번도 실행되지 않았다** — 저장소에 원격이 없어 첫 실행은 push 시점이다
- ~~**죽은 코드 정리**~~ **처리함 (커밋 `4146413`).** 세 파일을 지웠고,
  `recommendation_graph.py`가 유일한 사용처였던 `langgraph`도 `requirements.txt`에서
  뺐다(앞서 뺀 `reflex`와 함께 Render 빌드가 가벼워진다). 필요해지면 V2 저장소에
  그대로 있으니 다시 가져오면 된다

### 마이그레이션 적용 현황 (2026-08-18 기준)

| 파일 | 운영 적용 | 비고 |
|---|---|---|
| 001, 003 | 적용됨 | V2부터 |
| 004 (토큰 만료) | **적용됨** | 006만으로는 가입이 500이 나서 함께 적용했다 |
| 005 (username 필수) | **적용됨** | 2026-08-18. 계정 45개 삭제, 관리자(최지수) 한 행만 남음 |
| 006 (계정 정보·초기화) | **적용됨** | |
| 007 (사용 로그) | **적용됨** | 2026-08-18. 다섯 이벤트가 실제로 쌓이는 것까지 확인했다 |

**마이그레이션은 전부 적용됐다.** 005는 V3 배포가 올라간 뒤에 적용했다 - 먼저 적용하면
구버전 코드로 돌던 V2 배포의 로그인이 그 순간 깨지기 때문이다.

적용 전 백업은 `scripts/backup_before_005.py`로 떴다(93행). Supabase 무료 플랜에는 백업
기능이 없다("Free Plan does not include project backups").

적용 후 확인한 것: 남은 계정이 관리자 한 행이고 그의 재료 11개가 보존됐다.
`username`·`password_hash`에 NOT NULL이 걸렸고 `users_username_key` UNIQUE가 생겼다.
그 제약이 실제로 **같은 아이디 재가입을 409로 돌려보낸다** - `signup()`의
"확인 후 INSERT"가 경합에 지더라도 DB가 막아준다는 것이 이제 운영에서 검증됐다.

### Phase 2 — 디자인 확정 (코드 아님)

7절의 종료 조건을 채운다. 토큰과 시안까지만 하고 구현하지 않는다.

### Phase 3 — React 구현

- 프로젝트 구성, 디자인 토큰 적용, 인증 흐름
- 화면 이식 — V2의 단일 페이지를 **실제 라우트로 분해**한다
- **테스트를 코드와 같은 속도로 작성한다.** 나중에 몰아 쓰면 V2와 똑같이 절반이 빈다
- ~~사용 로그 수집을 이때 넣는다~~ **처리함 (2026-08-18).** `migration/007_usage_events.sql`
  + `api/usage_log.py`. 남기는 것과 남기지 않는 것, 그리고 Phase 4에서 이탈 지점을 보는
  쿼리는 007 파일 안에 적어뒀다. 운영 DB에 적용했고, 계정 하나로 가입부터 상세 열람까지
  태워 다섯 이벤트가 순서대로 쌓이는 것과 그 분석 쿼리가 도는 것까지 확인했다
  (검증에 쓴 계정은 005가, 그 계정들이 남긴 비로그인 열람 10행은 따로 정리했다.
  `usage_events`는 지금 비어 있으므로 Phase 4에 쌓이는 것은 전부 지인의 실제 행동이다)

### Phase 4 — 지인 5명 테스트

비용이 0이고, V2에서 끝내 못 한 유일한 검증이다.

5명에게 일주일 써보게 하고 DB에서 재료 입력 수, 추천 호출 수, 이탈 지점을 확인한다.
**첫 접속이 36초 걸릴 수 있다는 안내를 함께 보낸다** (무료 티어 콜드스타트).

V2는 84개 기능을 만들고 단 한 명에게도 검증받지 않았다. 같은 실수를 반복하지 않기
위한 단계다.

---

## 7. "디자인 완료"의 종료 조건

사용자의 원래 요구는 "실사용 테스트 전에 **완벽한** 디자인 개편"이었다. 그런데
"완벽한"에는 종료 조건이 없어서 그대로 두면 V3 기간의 대부분을 여기서 쓰게 된다.

게다가 **사용자 없이 하는 디자인 판단은 V2에서 이미 한 번 빗나갔다.** 접근성·일관성
개선(터치 타겟 44px, Pretendard 폰트, CTA 색 분리, 빈 화면 처리, 스켈레톤 로딩)을
다 했는데 "별로 크게 달라진 게 없는 것 같다"는 반응이 나왔고, 실제로 인상을 바꾼
건 그 뒤에 한 액센트 색 교체·아이콘 교체·카드 레이아웃 재배치였다.

**다만 "조악한 상태로 지인에게 보여주고 싶지 않다"는 판단 자체는 옳다.** 첫인상은
한 번뿐이고, 화면이 거칠면 핵심 가치가 아니라 겉모습에 대한 피드백만 돌아온다.

그래서 건너뛰지 않되, 아래를 **전부** 만족하면 완료로 본다.

1. ~~**디자인 토큰이 코드로 존재한다**~~ **처리함 (커밋 `f87cd3c`).**
   `design/tokens.css`에 색·타이포·간격·radius·그림자·레이아웃을 변수로 정의했다.
   "하드코딩된 값이 없다"는 프론트가 생기는 Phase 3에서 지켜야 할 규율이다
2. ~~**핵심 화면 5개 시안 확정**~~ **처리함.** `design/mockups/index.html`에
   홈·냉장고·추천 결과·레시피 상세·로그인 다섯 개를 그렸다. 정적 HTML이고 값은 전부
   토큰 변수를 쓴다. 데이터는 운영 DB의 실제 레시피라, 가짜 데이터로는 안 드러나는
   것들이 드러났다(아래 참고). 보려면 저장소 루트에서
   `python -m http.server 8002` 후 `/design/mockups/index.html`을 연다
3. ~~**다크 모드 양쪽 테마 검증**~~ **처리함.** 토큰에 다크 팔레트를 정의하고
   같은 명암비 검사를 양쪽 테마에 돌린다. 화면 검증은 2번과 함께 Phase 3에서
4. ~~**Lighthouse 접근성 90점 이상**~~ **처리함 (2026-08-18). 100점.**
   배포된 주소를 Chrome DevTools에서 모바일로 측정했다. 빨간·주황 항목 없이 22개 감사를
   통과했다. 뜬 항목 하나(`Identical links have the same purpose`)는 점수에 반영되지 않는
   모범사례 항목이고, 원인이 코드가 아니라 데이터다(아래 참고)
5. ~~**명암비 WCAG AA 통과**~~ **처리함.** `tests/test_design_tokens.py`가 토큰 파일을
   직접 읽어 검사한다(라이트·다크 각 19조합). 문서에 적어두는 대신 테스트로 만든 이유는,
   V2처럼 색을 조정하는 순간 문서가 낡기 때문이다. 변이 테스트로 검출력도 확인했다

### 디자인 근거 (Phase 2에서 확정한 것)

레퍼런스는 Figma 커뮤니티의 `Meal Planner App UI Kit`과 `Grocery App (Big Cart)`
두 파일이다(둘 다 CC BY 4.0 — 쓰면 출처 표기 의무). 구조만 참고하고 색·타이포는
우리 기준으로 새로 잡았다.

- **액센트는 새로 만들지 않았다.** `assets/manifest.json`의 `theme_color`와 `logo.svg`에
  이미 `#3f7d55`가 있었다. 명암비 검증 과정에서 `#3e7b54`로 1% 조정했다(로고 SVG는
  그대로 — WCAG는 로고타입을 명암비 대상에서 제외한다)
- **의미색은 표면용과 텍스트용을 나눴다.** 브랜드 주황 `#f2a53c`는 흰 배경에서
  2.02:1이라 아이콘 기준(3:1)조차 못 넘긴다. 채움으로만 쓰고 글자는 어두운 변형을 쓴다
- **카드는 1:1 정사각, 2열 격자다.** 레시피 이미지 1,146장이 전부 320×320이라
  와이드 히어로로 늘리면 2배 화면에서 흐려진다. 데이터가 비율을 정해준 셈이다
- **표면은 중립이다.** 사진 배경이 분홍·빨강·청록으로 제각각이라 앱 배경에 색을 넣으면
  부딪힌다. 색은 사진이 담당하고 UI는 액센트만 아껴 쓴다
- **이미지 URL은 `http://`다.** `https://`로 요청해도 같은 파일이 오는 것을 확인했으니
  렌더링 시 스킴만 바꾸면 된다. Phase 3 작업 항목

### 홈 화면을 만들면서 바꾼 것 (2026-08-13)

**시안의 "인기 레시피"를 "레시피 둘러보기"로 바꿨다.** 인기 순위는 좋아요 기준인데
운영 DB에 좋아요가 **4개**뿐이고 그마저 개발자 본인이 누른 것이라, 그 섹션은 카드
4장으로 끝난다. 1,148개를 검색·분류로 훑는 쪽이 지금 데이터로 성립한다. 좋아요가
쌓이면 그때 되살린다.

**목록 API가 화면을 지탱하지 못하는 상태였다.** 세 가지를 고쳤다.
- `RecipeSummary`에 `image_url`이 없었다. 카드가 사진 중심인데 목록이 사진을 안 주면
  레시피마다 상세를 한 번씩 더 불러야 한다 - 카드 20개면 요청 20번이고 콜드스타트가
  있는 무료 서버에서는 감당이 안 된다
- 검색에 `category`·`offset`이 없었다. 에이전트(`search_all_recipes`)는 지원하는데
  라우터가 열어주지 않아 분류 칩과 "더 보기"를 만들 수 없었다
- 분류 목록 엔드포인트가 없어 화면에 이름을 하드코딩해야 했다.
  `/recommendation/recipes/categories`를 추가했다(개수 포함)

### 시안을 실제 데이터로 그리면서 드러난 것 (Phase 3에서 처리)

- **`recipe_ingredients`에 구획 제목이 재료처럼 섞여 있다.** "주재료", "장식" 같은 행이
  `amount`/`unit` 없이 들어 있다. 재료 목록을 그냥 뿌리면 "주재료 —"가 재료 하나로 보인다.
  수량이 없는 행을 소제목으로 렌더링하는 게 맞다(시안은 그렇게 그렸다)
- **`steps_json`의 텍스트에 이미 번호가 붙어 있다.** "1. 검은콩을 …" 형태라 UI가 번호를
  또 붙이면 "1. 1. …"이 된다. 접두 번호를 떼거나 UI 번호를 안 붙여야 한다
- **조리 단계 이미지도 `http://`다.** 본 이미지와 같은 처리가 필요하다
- **이름이 같은 다른 레시피가 7쌍 있다** (2026-08-18, Lighthouse가 알려줬다).
  `가지볶음`(976/1048), `깻잎장아찌롤`, `둥지튀김`, `버섯 리조또`, `봄주먹밥`,
  `비타 오이 물김치`, `양배추롤`. **일곱 쌍 전부 조리법이 다르므로 중복 데이터가 아니다** —
  합치면 정보가 사라진다. 목록에서 이름·분류·칼로리가 같아 보이는 카드가 나란히 뜨는
  이유이고, 화면 낭독기로는 구분되지 않는다(Lighthouse의 unscored 항목).
  고친다면 중복 이름을 감지해 `aria-label`에 구분자를 붙이는 방식인데, "가지볶음 (1)"은
  구분은 되어도 의미를 주지 못한다. 지인 테스트에서 언급이 나오면 그때 판단할 것

"더 예뻐 보일 때까지"가 아니라 이 다섯 개가 기준이다.

### 홈을 테마 줄로 끊었다 (2026-08-18)

사용자 피드백이다. "홈 화면 추천 레시피가 두서 없고 어지럽다. 계절이나 제철, 한식·중식·
일식, 난이도 같은 테마로 정리하고 싶다."

**원인은 테마가 없는 것만이 아니었다.** 가나다순 20개가 한 덩어리로 쏟아져서
"가지겉절이, 가지나물냉국, 가지라따뚜이…"가 줄줄이 나오는 게 큰 몫이었다. 그래서
테마를 넣으면서 정렬을 **id순**으로 바꿨다 — 테마 안에서 같은 뭉침이 되풀이되면 의미가 없다.

**요청한 테마 중 둘은 데이터가 없어 만들지 못했다.**

- **한식·중식·일식** — `recipes`에 요리 종류 컬럼이 없다. 메뉴명으로 추측할 수는 있지만
  "가지 탕수육"을 어디에 넣을지가 어긋나기 시작하면 사용자가 먼저 알아챈다
- **난이도** — 컬럼이 없다. **재료 개수**로 대신했다(7개 이하 185개). 지어낸 등급보다
  정직하고 실제로 "간단한가"와 상관이 있다

만든 것은 넷이다. 8월 제철(32개, 달마다 바뀜), 간단하게(185), 가볍게 200kcal 이하(567),
든든하게 400kcal 이상(163). 걸리는 레시피가 없는 테마는 빈 줄을 보여주느니 줄째 뺀다.

`GET /recommendation/recipes/themes`가 네 줄을 **한 번에** 돌려준다. 줄마다 따로 부르면
무료 티어에서 왕복이 네 배가 되고 콜드스타트까지 겹치면 첫 화면이 그만큼 늦어진다.

검색어나 분류가 걸려 있으면 테마를 감춘다. 찾는 게 분명한 사람에게 테마는 방해다.

### 추천 화면에서 기준 재료를 고칠 수 있게 했다 (2026-08-18)

사용자 요청이다. "냉장고에 저장된 재료와 별개로 추천 탭에서 간단하게 재료를 넣고
추천하고 싶다. 지금은 수정하려면 냉장고로 가서 실제 재료를 다 바꿔야 한다."

**V2에 있던 기능인데 V3 이식 과정에서 잃어버린 것이었다.** 백엔드는 그대로였다 —
`api/routers/recommendation.py`의 주석에 2026-07-19자로 "이번 추천에만 쓸 재료를
그때그때 직접 구성하고 싶다"는 같은 요청 때문에 pantry 자동 조회를 없애고 호출부가
넘긴 목록을 쓰도록 바꿨다고 적혀 있다. V3 프론트가 냉장고를 읽어 그대로 넘기기만 하고
사용자가 손댈 방법을 주지 않았을 뿐이다. **그래서 이번 변경은 프론트 전용이다.**

설계에서 판단이 필요했던 것은 언제 다시 부를지다. 칩을 지울 때마다 자동으로 부르면
편집 한 번에 2.4초씩 기다리게 된다(무료 티어 실측). 그래서 편집은 즉시 반영하되
호출은 "이 재료로 추천받기" 버튼으로 분리했고, 그 버튼은 **지금 목록이 화면의 결과와
다를 때만** 나타난다. 항상 있으면 눌러야 하는지 아닌지가 안 보인다.
"냉장고 재료로 되돌리기"도 같은 원칙으로 편집한 뒤에만 나타난다.

편집은 이번 추천에만 쓰이고 냉장고를 건드리지 않는다. 브라우저로 확인했다 — 양파를
빼고 추천을 다시 받은 뒤에도 DB의 `ingredients`에는 양파가 그대로 있었다.

### 온보딩 화면을 만들면서 드러난 것 (2026-08-18)

**알레르기 제외가 그때까지 한 번도 작동한 적이 없었다.** `PUT /profile/{user_id}`와
`has_profile`은 준비돼 있었는데 프론트에 입력 화면이 없어서 `users.allergy`가 계속
`NULL`이었다. 가입만 하고 추천을 받으면 알레르기 필터를 그냥 통과했다. 지인 테스트에서
알레르기가 있는 사람이 있었다면 그대로 사고였다.

**태그에 동의어가 갈라져 있다.** 운영 DB의 `recipe_tags`는 `달걀`(200개)과
`계란`(27개), `소고기`(71개)와 `쇠고기`(47개)를 서로 다른 태그로 들고 있다. 정확히
일치하는 값만 걸러내면 `달걀`을 고른 사람에게 `계란` 태그 레시피 27개가 그대로 나간다.
`recommendation_agent.ALLERGY_SYNONYMS` + `expand_allergies()`로 확장해서 거른다.
실제 데이터로 확인했다 - 후보 832개 중 금지 태그가 새어나온 레시피는 0개이고,
`계란`만 붙은 27개도 전부 빠졌다.

**알레르기는 자유 입력을 받으면 안 된다.** 기존 값에 `콩`(태그는 `대두`)이나 `@$#$`
같은 게 남아 있었다. 그렇게 저장되면 사용자는 골랐다고 믿는데 필터는 아무것도 안 거른다.
`GET /profile/allergy-options`가 실제 태그에서 목록을 만들어 주고, 화면은 거기서만
고르게 한다(현재 20개).

**프로필이 도착하기 전에 폼을 내주면 안 된다.** 브라우저로 태워보다 실제로 겪었다.
칩을 골라둔 상태에서 `GET /profile`이 늦게 도착하면 초기값이 선택을 덮어써 조용히
비워진다. 게다가 `PUT`은 프로필 전체를 덮어쓰기 때문에, 가입 때 받은 성별·연령대를
모르는 채 저장하면 그 값이 빈 문자열로 날아간다. 그래서 프로필 도착 전에는 로딩만
보여준다.

---

## 8. V2에서 배운 함정 (반드시 읽을 것)

같은 실수를 반복하지 않기 위한 목록이다. 전부 실제로 겪은 것이다.

### 8.-6 사용자에게 보이는 기준값이 도달 불가능하면 기능이 꺼진 것이다 (2026-08-19)

유저가 등록한 레시피는 추천이 `USER_RECIPE_MIN_LIKES`회 쌓여야 다른 사람의 추천 후보가
된다(`recommendation_agent.py`). 그 값이 **100**이었는데, V2를 28일 돌리는 동안 서비스
전체에 쌓인 추천이 **4개**였다. 레시피 하나에 100개는 그때까지 나온 전체의 25배다.

**승격이 한 번도 일어나지 않는 기준은 품질 장치가 아니라 기능을 끄는 스위치다.** 3으로
내렸다. 상수 하나라 데이터가 쌓이면 올리기 쉽다.

같은 종류의 함정을 새로 만들 때 확인할 것: **기준값을 정할 때 그 값이 지금 데이터
규모에서 도달 가능한지 세어 본다.** 그럴듯한 숫자를 고르면 기능이 조용히 꺼진다.

### 8.-5.1 추천하기와 즐겨찾기는 다른 기능이다 (2026-08-19)

테이블이 둘로 갈라져 있다.

- `recipe_likes` — **추천하기**. 남에게 보이는 공개 지표. 홈의 "많이 추천한 메뉴"에
  쓰이고, **유저 레시피 승격은 이쪽만 센다**
- `favorites` — **즐겨찾기**. 나만 보는 목록

화면은 한동안 즐겨찾기만 "저장하기"라는 이름으로 붙여 놓고 있었다. 새 화면을 만들 때
둘을 같은 아이콘·같은 말로 그리지 말 것 — 사용자가 즐겨찾기를 눌러놓고 남을 도왔다고
오해하는데, 실제로 승격에는 아무 영향이 없다. 지금은 ♥(추천, 누적 수 표시)와
★(즐겨찾기)로 갈라 뒀다.

### 8.-5 화면이 서버의 값을 따로 들고 있으면 조용히 어긋난다 (2026-08-19)

같은 사고가 하루에 세 번 났다. 셋 다 "규칙이 두 곳에 있었다"가 원인이다.

- **가입 선택지** — 화면이 `50대`·`60대 이상`을 주는데 영양 기준표는 `50대 이상`만 알았다.
  그걸 고른 사람은 영양 분석을 아예 못 받는데 **화면은 저장됐다고 말했다**
- **병력 정보** — 서버는 정해진 다섯 개만 알아듣고 나머지를 조용히 버리는데 화면은 자유
  입력이었다. "혈압 높음"이라고 쓰면 고혈압 나트륨 조정이 안 걸린다
- **재료인지 구획 제목인지** — 화면이 "수량 없으면 제목"으로 판단했다. 아래 8.-4 참고

**판정 규칙은 서버가 갖고, 화면은 받아 쓴다.** 알레르기에서 먼저 이렇게 했는데
(`GET /profile/allergy-options`) 나머지에 적용하지 않아 같은 사고를 반복했다. 지금은
`GET /profile/options`가 성별·연령대·병력을 내려주고, 레시피 상세가 `kind`로 재료 여부를
알려준다. 고를 수 있는 모든 값에 실제로 기준이 있는지도 테스트로 고정했다
(`tests/test_profile_options.py`).

### 8.-4 원본 데이터의 구획 제목이 재료로 새어 들어온다 (2026-08-19)

`recipe_ingredients`와 `recipe_tags` 양쪽에 "주재료"·"장식" 같은 **구획 제목이 재료처럼**
들어 있다. 이게 세 군데를 동시에 망가뜨리고 있었다.

- **화면에서 재료가 사라졌다.** "수량이 없으면 제목"이라는 판정이 틀렸다 - 수량 없는 행
  649개 중 진짜 제목은 4개뿐이고 나머지는 "소금적당량"처럼 수량이 글자로 적힌 재료다.
  309개 레시피(27%)가 영향받았고, 그 행이 목록 끝이면 그룹째 버려져 **소금이 사라졌다**
- **부족한 재료 목록이 "주재료가 부족합니다"라고 말했다**
- **추천 순서가 틀어져 있었다.** `missing_count`가 정렬 기준인데 있지도 않은 재료를 셌다

판정은 `portion_agent.classify_ingredient_row()` 하나가 갖는다. 재료 이름을 다루는 코드를
새로 쓸 때는 이 함수를 거칠 것.

**표시용 숫자와 정렬용 숫자를 구분할 것.** 정렬은 조미료를 빼고 세는 게 맞지만(변별력이
없다) 그 숫자를 화면에 쓰면 소금이 없는데도 "다 있다"가 된다. 두 화면이 같은 레시피를
다르게 말하면 둘 다 못 믿게 된다.

### 8.-3 Render 무료 인스턴스는 SMTP를 막는다 (2026-08-18)

비밀번호 초기화 메일이 배포에서 안 나갔다. **자격증명 문제가 아니다.** Render 무료 웹
서비스가 SMTP 포트(25/465/587) 아웃바운드를 차단한다(2025-09-26부터 전 리전).
`mail_agent`는 465를 쓴다.

원인을 가른 방법을 남겨둔다. 서버 로그에서 `POST /auth/password-reset/request`가
**21초** 걸린 것이 단서였다 - `SMTP_TIMEOUT_SECONDS = 20`과 맞는다. 자격증명이 틀리면
1~2초 만에 인증 실패로 끝나지 20초를 기다리지 않는다. 즉 TCP 연결 자체가 안 맺어진
것이다. 확인 사살로 **같은 코드·같은 자격증명을 로컬에서 돌려 2.5초에 발송 성공**시켰다.

Phase 4 동안의 결정: **메일 경로를 쓰지 않는다.** `/forgot-password`에서 입력 폼을 빼고
"만든 사람에게 연락해달라"고 그대로 말한다. 폼을 남겨두면 "링크를 보냈어요"라고
말해놓고 아무것도 안 보내는 화면이 되는데, 실패가 사용자에게 성공으로 보이는 것이
가장 나쁘다. 서버 엔드포인트와 `/reset-password` 화면은 살려뒀고,
`scripts/make_reset_link.py`로 링크를 직접 만들어 전달하면 초기화는 지금도 된다
(운영에서 확인: 새 비밀번호 로그인 200, 옛 비밀번호 401, 토큰 재사용 400).

되살리려면 유료 인스턴스로 올리거나 HTTPS 메일 API(SendGrid·Brevo 등)로 바꾸면 된다.
`mail_agent.send_mail()` 내부만 바꾸면 호출부는 그대로다.

### 8.-2 정적 사이트 빌드가 devDependencies 없이 돌아간다 (2026-08-18)

`tsc`와 `vite`가 둘 다 `devDependencies`에 있는데, `NODE_ENV=production`인 환경에서
`npm ci`는 devDependencies를 건너뛴다. 그러면 `'tsc'을(를) 찾을 수 없습니다`로 빌드가
죽는다. 로컬에서 `NODE_ENV=production npm ci && npm run build`로 그대로 재현했고,
`npm ci --include=dev`로 바꾸니 통과했다.

**빌드 명령에 `--include=dev`를 유지할 것.** `NODE_ENV`가 안 걸린 환경에서는 아무 차이가
없으므로 붙여둬도 손해가 없다. `tests/test_deploy_config.py`가 이 플래그를 지킨다.

또 하나, 언어 버전을 고정했다. 루트 `.node-version`(24)과 `PYTHON_VERSION=3.12`가
CI와 같은 값이다. 빼두면 Render의 그때그때 기본값을 쓰게 되는데, 코드가 검증된 조합은
그 둘뿐이다.

### 8.-1 배포 설정이 코드보다 뒤처져 조용히 죽는 기능 (2026-08-18)

`render.yaml`이 넘겨주던 환경변수는 5개인데 `api/`·`src/`가 읽는 것은 15개였다. 빠진 것
중 셋이 비밀번호 초기화 경로였고, 이게 특히 나빴던 이유는 **실패가 어디에도 드러나지
않기 때문**이다. `/auth/password-reset/request`는 계정 존재 여부가 새어나가지 않도록
성공·실패에 관계없이 같은 응답을 준다(그 자체는 옳은 설계다). 그래서 자격증명이 없으면
사용자는 "메일을 보냈어요"를 보고 기다리는데 아무것도 오지 않고, 응답에도 화면에도
신호가 없다. `WEB_BASE_URL`은 기본값이 `http://localhost:5173`이라, 메일이 나가더라도
링크가 받는 사람의 컴퓨터를 가리킨다.

**배포 설정과 코드의 어긋남은 사람이 눈으로 맞추지 말 것.** `tests/test_deploy_config.py`가
`render.yaml`의 선언과 코드가 실제로 읽는 이름을 대조한다. 새 환경변수를 읽는 코드를
쓰면 이 테스트가 먼저 깨진다.

### 8.0 부수적인 쓰기가 사용자의 진짜 쓰기를 삼킨다 (2026-08-18)

사용 로그를 넣으면서 마주친 함정이다. **로그 INSERT를 try/except로 감싸는 것만으로는
부족하다.** Postgres는 트랜잭션 안에서 문 하나가 실패하면 트랜잭션 전체를 중단 상태로
만들고, 그 상태의 `COMMIT`은 사실상 `ROLLBACK`으로 처리된다. 즉 예외를 조용히 삼켜도
**사용자가 방금 넣은 재료가 응답은 200인 채로 사라진다.**

이게 특히 고약한 이유는 sqlite에서는 재현되지 않는다는 점이다. sqlite는 문 하나가
실패해도 트랜잭션을 그대로 두므로 로컬 테스트는 전부 초록이다(8.3과 같은 구조의 함정).

**부수적인 쓰기는 `SAVEPOINT`로 감쌀 것.** 실패해도 되돌리는 범위가 그 한 줄로 한정된다.
`api/usage_log.py`가 그렇게 되어 있고, 운영 Postgres에서 확인했다 - 세이브포인트가
있으면 앞선 쓰기가 살아남고, 빼면 뒤따르는 조회가 `InFailedSqlTransaction`으로 터진다.

비용도 재봤다. Supabase 왕복 1회가 6.4ms이고 `SAVEPOINT + INSERT + RELEASE`가 19.1ms다
(왕복 3회). 추천 웜 응답 2.6초의 0.7%라 최적화할 이유가 없다.

### 8.1 psycopg2에서 SQL 안의 리터럴 `%`가 깨진다

`api/deps.py`의 `SqliteStyleCursor`가 sqlite 스타일 `?`를 `%s`로 변환하는데,
SQL 문자열에 박힌 리터럴 `%`(예: `LIKE '%말린%'`)를 파라미터 플레이스홀더로
오인해서 `IndexError: tuple index out of range`가 난다.

**LIKE 패턴은 문자열에 박지 말고 파라미터로 바인딩할 것.**

### 8.2 FastAPI 라우트 순서

같은 라우터에서 `/{recipe_id}`(int) 같은 파라미터 경로가 먼저 등록되면
`/popular`, `/search`, `/demo` 같은 리터럴 경로가 422로 실패한다.
**리터럴 경로를 파라미터 경로보다 먼저 등록할 것.**

### 8.3 테스트는 sqlite, 운영은 Postgres

이 괴리 때문에 8.1의 버그가 로컬 테스트 전부 통과 상태로 프로덕션에 나갔다.
**Phase 1에서 CI에 Postgres를 붙이는 이유가 이것이다.**

### 8.4 어떤 agent가 이미 API로 감싸졌는지 확인하는 법

**파일 이름만 비교하면 놓친다.** 각 라우터 파일의 **import 문 안쪽까지** 봐야 한다.
V2에서 실제로 `ingredient_catalog.py` 안에 있던 `ingredient_favorite_agent`
엔드포인트를 놓친 적이 있다.

### 8.5 Render 콜드스타트는 버그가 아니다

무료 웹 서비스는 15분 유휴 후 중단되고 첫 요청에 20~30초가 걸린다
(V3 실측 22.9초, V2 코드일 때 32.3초 / 36.3초). V3가 빨라진 것은 `reflex`와
`langgraph`를 의존성에서 뺀 덕으로 보인다. 정적 사이트는 CDN이라 해당 없다.
**프로덕션 검증 중 첫 요청이 타임아웃 나면 버그를 찾기 전에 재시도부터 할 것.**

측정에 두 번 실패하고서야 제대로 쟀다. 재는 동안 **그 서비스를 깨우는 모든 행위**를
멈춰야 한다. 두 가지가 걸린다.

- 확인차 API를 한 번 부르는 것. 유휴 타이머가 그 순간 초기화된다
- **`main`에 push하는 것.** 자동 배포가 서비스를 재시작시킨다. 이쪽을 놓쳐서 두 번을
  버렸다 — 창 한가운데서 커밋을 올려놓고 1.5초라는 결과에 한참 갸웃했다

두 번 다 1.5초 언저리로 "일관되게" 나온 것이 오히려 함정이었다. 재배포 직후의 웜
응답이라 값이 안정적이었던 것이고, 일관성은 정확성의 근거가 아니다.

### 8.5.1 로컬에서 `localhost`로 재면 2초가 얹힌다 (2026-08-18)

로컬 API가 모든 요청에 2.0초씩 걸려서 새로 만든 엔드포인트가 느린 줄 알았다. 아니었다.

| 주소 | `/openapi.json` (DB를 안 쓴다) |
|---|---:|
| `http://localhost:8001` | 2038ms |
| `http://127.0.0.1:8001` | **4ms** |

Windows에서 `localhost`가 IPv6(`::1`)로 먼저 풀리는데 uvicorn은 IPv4에만 바인딩돼 있어,
매 요청이 IPv6로 붙다 실패한 뒤 IPv4로 재시도한다. 그 재시도 대기가 2초다.

**로컬 성능을 잴 때는 `127.0.0.1`을 쓸 것.** DB를 안 쓰는 엔드포인트도 똑같이 2초가
나오는지 보면 이 문제인지 바로 갈린다. 브라우저는 이 지연을 겪지 않는 경우가 많아
"코드는 멀쩡한데 측정만 느린" 상태가 된다.

### 8.6 다단계 명령의 출력을 자르지 말 것

V2에서 배포 실패 원인을 **세 번 연속 잘못 지목**했는데, 세 번 다 `tail`이나 파일
리다이렉트로 출력을 잘라 보면서 서버가 보낸 실제 실패 메시지를 놓친 탓이었다.
출력을 자르지 않고 다시 돌리자 원인이 첫 줄에 있었다.

### 8.7 외부 API는 불안정하다

식약처 안전 API(`safety_agent.py`)는 간헐적으로 무응답이고, 이미 503으로 우아하게
처리하도록 고쳐뒀다. YouTube Data API는 일일 할당량이 있어서 대량 작업은 나눠
실행해야 한다.

### 8.8 파이썬은 반드시 가상환경 것을 쓸 것

시스템 파이썬으로 `pytest`를 돌리면 `psycopg2` 없음으로 수집 단계에서 실패한다.
V2에서는 `./.venv/Scripts/python.exe -m pytest`로 실행했다.

---

## 9. 새 폴더로 가져갈 것 / 두고 갈 것

**이 절은 2026-08-12에 실행이 끝났다.** 무엇을 왜 가져왔는지 판단 근거로 남겨둔다.
지금 저장소 구성은 `README.md`를 볼 것.

### 가져갈 것

| 대상 | 이유 |
|---|---|
| `api/` | FastAPI 라우터 53개 — V3 프론트가 그대로 소비 |
| `src/agents/` | 도메인 로직 (죽은 코드 3개는 Phase 1에서 정리) |
| `tests/` | 당시 67개 통과 중. 여기에 admin 테스트를 더한다 (현재 214개) |
| `scripts/` | 유튜브 백필 등 운영 스크립트 |
| `migration/`, `data/` | 스키마 DDL과 시드 |
| `assets/` | 로고 등 브랜드 자산 |
| `requirements.txt`, `requirements-dev.txt`, `pytest.ini` | 백엔드 환경 |
| `.github/workflows/` | CI. Phase 1에서 Postgres 추가 |
| `.env` | **값이 들어 있으니 직접 복사할 것. 저장소에 커밋 금지** |
| `PROJECT_REPORT_2026-08-12.md` | V2 진단 근거. 판단이 필요할 때 참조 |
| 이 문서 (`V3_HANDOFF.md`) | — |

### 두고 갈 것

| 대상 | 이유 |
|---|---|
| `naengjango_v2/` | Reflex 프론트 4,047줄 — React로 대체된다. 다만 **이식 중 화면 명세로 참조할 가치가 있으니 V2 폴더는 지우지 말 것** |
| `rxconfig.py`, `reflex.lock`, `.web/` | Reflex 전용 |
| `hf_space/` | 허깅페이스 데모 시도 잔재 (무료 계정은 PRO 필요로 막힘) |
| `DEPLOY_ISSUE_2026-08-11.md` | Reflex Cloud 장애 기록. 이력용이라 V2에 남겨둠 |
| `uploaded_files/`, `backup/`, `__pycache__/` | 런타임 산출물 |

### 환경변수 (이름만 — 값은 V2의 `.env`에서 직접 복사)

실제로 쓰이는 것부터 적었다.

```
POSTGRES_URL          # Supabase 연결 문자열 (필수)
OPENAI_API_KEY        # 후기 요약, 냉장고 사진 재료 인식
FOODSAFETY_API_KEY    # 식약처 안전정보
INGREDIENT_API_KEY    # 식약처 영양성분DB
NUTRITION_API_KEY     # 식약성분
YOUTUBE_API_KEY       # 인기 영상, 레시피 영상 백필
KAMIS_CERT_ID         # 농산물 가격
KAMIS_CERT_KEY
ADMIN_SECRET          # 관리자 승격
APP_ENCRYPTION_KEY    # 암호화 유틸
API_BASE              # 프론트가 호출할 FastAPI 주소
```

`COUPANG_*`, `GMAIL_*`, `NOTIFY_EMAIL_TO`, `RECALL_API_KEY`, `TAVILY_API_KEY`,
`ANTHROPIC_API_KEY`, `GEMINI_API_KEY`는 현재 실제로 쓰이지 않거나 미승인 상태다.
정리 대상.

---

## 10. 지금 할 일

**이 절의 원래 내용(저장소 초기화, 백엔드 복사, 첫 커밋)은 전부 끝났다.** 이력은 git에
있으므로 지우고, 지금 시점에 남은 것만 적는다.

**7절 종료 조건 다섯 개가 전부 채워졌다. 남은 것은 Phase 4 하나다.**

> **2026-08-19 추가 작업.** 전수 검토에서 반쯤 만들다 만 것과 결함이 여럿 나와 먼저
> 고쳤다. 서버에는 있는데 화면이 안 부르던 기능(영양 분석·부족한 재료·대체 재료·재료
> 자동완성·즐겨찾기)을 이었고, 그 과정에서 재료가 화면에서 사라지던 버그와 추천 순서가
> 틀어져 있던 것을 찾아 고쳤다. 자세한 것은 8.-5, 8.-4 참고.
>
> **2026-08-19 두 번째 작업 — V2 기능 4종을 화면에 이었다.** 레시피 등록·재료 등록·
> 리뷰·추천하기다. 넷 다 서버 코드와 엔드포인트는 V2 때부터 있었고 React 화면만 없었다.
> 엔드포인트 51개 중 화면이 부르는 것이 17개에서 **34개**가 됐고, 화면은 12개다.
>
> 한 번 "지인 테스트 뒤로 미루자"고 판단했다가 사용자가 뒤집었고 그 판단이 맞았다.
> 미루자는 근거가 "추천이 4개뿐"이었는데, **4개인 이유가 화면에 버튼이 없어서**였다.
> "쓰이지 않으니 나중에"라고 말하기 전에 그 쓰이지 않음이 어디서 왔는지 볼 것.
>
> 함께 정한 것: 유저 레시피 승격 기준 100 → **3**(전체 추천이 4개인데 100은 도달 불가),
> 추천하기(♥ 공개)와 즐겨찾기(★ 비공개) 분리, 후기 AI 요약은 비용 때문에 보류.
> 자세한 것은 8.-6 참고.
>
> **아직 화면이 안 부르는 것 17개** — 가격·쇼핑·안전정보·제철·인기영상·재료 즐겨찾기·
> 후기 AI 요약. 지인 테스트에서 요청이 나오면 잇고, 안 나오면 지운다.

**1) Phase 4 — 지인 5명 테스트.** 준비물은 `docs/PHASE4_지인테스트.md`에 있다.
보낼 메시지, 성능 실측치, 끝나고 볼 분석 쿼리 세 개(운영 DB에서 실행 확인함)가 들어 있다.
개발 중 검증 흔적은 지웠으므로 `usage_events`에 남은 것은 전부 실제 사용 기록이다.
다만 **완전히 빈 상태는 아니다** — 링크를 뿌리기 전인 8월 18일에 가입한 사람이 한 명
있고 24행을 남겼다. 집계할 때 `users.created_at`으로 가른다.

**2) 테스트 중 지켜볼 것.** `main`에 push하면 자동 배포된다. **push한 뒤에는 CI 결과를
반드시 확인할 것** — 8월 19일에 프론트 테스트 픽스처 누락으로 CI가 두 번 빨갛게 떴는데
그동안 다음 작업을 하고 있었다. 로컬 `tsc --noEmit`은 테스트 파일을 검사하지 않으므로
`npm run build`나 CI만 잡는다. 지인이 쓰는 동안에는
push 전에 한 번 더 생각할 것 — 빌드가 실패하면 그동안 사이트가 옛 버전으로 남는다.
배포 상태는 `scripts/smoke_test_deploy.py`로 언제든 확인한다 — **35항목**으로 가입부터
추천·즐겨찾기·후기·레시피 등록까지 지인이 밟을 흐름을 실제로 태운다. 흔적을 남기지
않는다: 검증 계정을 스스로 만들고 지우고, 공개 레시피에 누른 추천은 확인 후 다시 눌러
되돌리며, 등록한 레시피는 삭제 경로로 지운다(그 삭제가 검사 항목이기도 하다).
**새 기능을 붙이면 여기에도 항목을 추가할 것** — 경로 개수만 대조하는 검사는 응답에
필드가 늘어난 것을 못 잡는다(그래서 `is_admin` 확인을 따로 넣었다).

비밀번호를 잊었다는 연락이 오면 `scripts/make_reset_link.py`,
계정 삭제 요청은 `scripts/delete_account.py`다.

**3) 테스트가 끝나면.** 그때 나온 데이터가 이후 모든 판단의 근거다. 그전까지는 기능을
추가하지 않는다(4절). 미뤄둔 것들은 이 문서 곳곳에 "지인 테스트에서 언급이 나오면"으로
적어뒀다 — 추천 2.4초, 이름이 겹치는 레시피 7쌍, 초기화 메일 경로가 그렇다.

### 참고 — 성능 기준선

**Render 실측 (2026-08-18, V3 배포 기준)**

| 항목 | 값 | 비고 |
|---|---:|---|
| 정적 사이트 | 0.29초 | CDN, 스핀다운 없음 |
| API 콜드스타트 | 22.9초 / 33.0초 | 유휴 후 2회(08-18, 08-19). 재는 법은 8.5 참고 |
| 추천 웜 응답 (`/recommendation/{user_id}`) | 2.40초 | 5회 중앙값 |
| 레시피 검색 웜 응답 | 0.61초 | |

**Phase 1의 "추천 1초 미만" 목표는 달성하지 못했다.** 2.4초다. 다만 로컬에서 이미
0.674초 → 0.20초까지 줄인 뒤의 숫자라, 남은 것은 알고리즘 자체의 몫이다. 무료 티어가
**0.1 CPU**여서 CPU 바운드 구간만 증폭되는 구조이고, 같은 서버에서 DB 바운드인 검색은
0.61초로 멀쩡하다. 로딩 표시가 있으면 2.4초는 견딜 만하므로 **지인 테스트 전에 더
손대지 않기로 했다.** 진짜 문제는 첫 접속의 30초대다.

**참고 — 2026-08-12 V2 배포 기준 (비교용)**: 콜드스타트 36.3초, 추천 2.6초,
검색 0.41초.

**로컬 실측 (2026-08-12, 캐시 적용 전후)**

| 구간 | 적용 전 | 적용 후 |
|---|---:|---:|
| 전체 | 0.674초 | **0.20초** |
| └ DB 왕복 | 0.165초 | 0.135초 |
| └ 파이썬 | 0.509초 | **0.074초** |
| 서버측 쿼리 (`EXPLAIN ANALYZE`) | 73.7ms | 73.7ms |

로컬 DB 왕복에는 Supabase까지의 네트워크가 섞여 있어 회차별로 크게 튄다. 쿼리
자체가 느린지 판단하려면 왕복 시간이 아니라 서버측 실행 시간을 봐야 한다.

---

## 11. 이 문서 외에 참고할 것

| 문서 | 내용 |
|---|---|
| `README.md` | 프로젝트 소개와 개발 명령. 저장소에 처음 오는 사람용 |
| `docs/PHASE4_지인테스트.md` | 지인에게 보낼 메시지와 끝나고 볼 분석 쿼리 |
| `PROJECT_REPORT_V3.md` | V3 종합 보고서. 무엇을 만들었고 어떤 진단이 틀렸는지 |
| `PROJECT_REPORT_2026-08-12.md` | V2 종합 보고서. 달성률, 문제와 해결, 개선방안을 실측 기반으로 정리 |
| `migration/*.sql` | 각 파일 머리말에 그 마이그레이션이 필요했던 이유가 적혀 있다 |
| `scripts/` | 운영 도구. 배포 점검·005 백업·초기화 링크 생성 |
| `V3_PLAN.md` | 2026-08-12 착수 계획. 갱신하지 않고 기록으로 둔다 — 다만 그 뒤 측정으로 틀린 것이 드러난 판단 셋에는 정정을 달아뒀다 |
| `DEPLOY_ISSUE_2026-08-11.md` | Reflex Cloud 장애와 Vercel+Render 이전 경과 (V2 폴더에 있음) |
| V2 저장소 `naengjango_v2/naengjango_v2.py` | 화면 명세로 참조. 컴포넌트 81개, 이벤트 핸들러 67개 |

---

*수치에는 측정일을 함께 적었다. 날짜가 없는 것은 2026-08-12 기준이다.
Vercel·Render 요금과 약관은 그때 확인했으며 정책은 변경될 수 있다 — 실제로 Render의
SMTP 차단(8.-3)이 그렇게 바뀐 정책이다.*
