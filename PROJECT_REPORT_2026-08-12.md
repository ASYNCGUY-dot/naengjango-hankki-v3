# 냉장고 한끼 V2 — 프로젝트 종합 보고서

작성일: 2026-08-12
대상 커밋: `b8e349d`
작성 기준: 코드베이스 실측 + 운영 DB 직접 조회 + 프로덕션 응답 실측

> 이 보고서는 잘된 점을 홍보하기 위한 문서가 아니다. 다음 단계를 판단하는 데
> 쓰려고 쓴 문서이므로, 불리한 수치도 그대로 적었다. 모든 숫자는 추정이 아니라
> 2026-08-12에 직접 측정한 값이다.

---

## 1. 전체 개요

### 무엇을 만들었나

냉장고에 있는 재료를 입력하면 공공데이터 기반 레시피 1,148개 중에서 그 재료를 가장
잘 활용하는 메뉴를 추천하는 모바일 우선 웹앱(PWA)이다. 추천에 더해 재료 안전정보
확인, 가격 비교, 제철 재료 안내, 사용자 레시피 등록·승인, 후기, 즐겨찾기까지
붙어 있다.

V1(별도 프로젝트)의 자산을 이어받아 재구축한 두 번째 버전이며, V1에서 넘어온
8개 에이전트 구조와 공공데이터 6종을 기반으로 한다.

### 규모 (2026-08-12 실측)

| 영역 | 파일 | 코드 라인 |
|---|---:|---:|
| Reflex 프론트엔드 | 2 | 4,047 |
| FastAPI 라우터·스키마 | 22 | 2,043 |
| 도메인 에이전트 | 27 | 4,844 |
| 테스트 | 11 | 1,096 |
| 운영 스크립트 | 2 | 136 |
| **합계** | **64** | **12,166** |

개발 기간은 2026-07-16 첫 커밋부터 2026-08-12까지 **28일**, 커밋 **67개**다.
API 엔드포인트는 18개 라우터에 걸쳐 **53개**다.

### 데이터 자산

이 프로젝트에서 가장 대체하기 어려운 자산은 코드가 아니라 정제된 데이터다.

| 테이블 | 행 수 | 비고 |
|---|---:|---|
| `ingredient_catalog` | 302,629 | 식약처 식품영양성분DB |
| `recipe_tags` | 16,315 | 레시피별 재료·알레르기 태그 |
| `recipe_ingredients` | 12,965 | 수량 정보 포함 |
| `recipes` | 1,148 | 조리법·영양정보 |
| `popular_videos` | 25 | 유튜브 인기 영상 |

레시피 1,148개 중 **1,125개(98%)에 유튜브 조리 영상 링크가 채워져 있다.** 나머지
23개는 검색해도 영상이 없어 "시도했으나 없음"으로 표시된 상태다. 이 백필은 유튜브
API 일일 할당량 때문에 하루 50개씩 나눠 자동 실행했고, 이제 완료돼 예약 작업도
스스로 삭제됐다.

### 현재 배포 구성

| 구성 | 호스팅 | 주소 |
|---|---|---|
| 프론트엔드 (정적 번들) | Vercel | `naengjango-hankki-demo.vercel.app` |
| Reflex Python 백엔드 | Render 무료 | `naengjango-hankki-v2-reflex.onrender.com` |
| FastAPI 백엔드 | Render 무료 | `naengjango-hankki-v2-api.onrender.com` |
| 데이터베이스 | Supabase (Postgres) | — |

---

## 2. 목표지점

이 프로젝트가 원래 겨냥한 것은 세 가지였다고 정리할 수 있다.

**첫째, 실사용 가능한 서비스.** "냉장고에 있는 재료로 뭘 해먹지"라는 실제 문제를
해결하는 앱. 로그인해서 냉장고를 관리하고, 추천을 받고, 후기를 남기는 순환이
돌아가는 것.

**둘째, 포트폴리오·공모전 제출물.** 공공데이터 활용, 다중 에이전트 아키텍처,
LLM 결합, PWA 배포까지 기술적 폭을 보여주는 결과물.

**셋째, V1 자산의 완전한 이식.** 8개 에이전트 구조, 공공데이터 6종, 안전정보
자동확인을 V2에서도 유지하는 것.

이 셋은 요구사항이 겹치지만 **완전히 같지 않다.** 뒤에서 보겠지만, 지금까지의
작업은 둘째 목표에 거의 전부 정렬돼 있고 첫째 목표는 사실상 손대지 않았다.
이 어긋남이 이 보고서의 핵심 논점이다.

---

## 3. 달성률

### 기능 구현 기준: 약 95%

작업 목록 84개가 전부 완료 상태다. 로드맵에 올렸던 기능 중 미구현으로 남은 것은
없다. 안 붙인 것은 의도적으로 범위 밖으로 뺀 것들이다(제휴 수수료 연동, 알림 발송).

### 영역별 실측

| 영역 | 상태 | 근거 |
|---|---|---|
| 백엔드 API | **완료** | 엔드포인트 53개, 전부 프로덕션 응답 확인 |
| 프론트 화면 | **완료** | 5탭 전 화면 구현, 브라우저 회귀 검증 완료 |
| 데이터 적재 | **완료** | 레시피 1,148, 영양DB 302,629행 |
| 인증·인가 | **완료** | 토큰 인가, 관리자 엔드포인트 무인증 접근 시 401 확인 |
| 보안(RLS) | **완료** | 16개 테이블 RLS 활성화 |
| 자동화 테스트 | **부분** | 67개 통과. 다만 커버 범위가 절반 (5절 참고) |
| 배포 | **완료** | 3개 서비스 전부 정상, `/demo`에서 추천 5건 반환 확인 |
| **실사용** | **0%** | **아래 참고** |

### 냉정하게: 실사용 달성률은 0에 가깝다

운영 DB를 직접 조회한 결과다.

| 지표 | 값 |
|---|---:|
| 가입 사용자 | 43 |
| 그중 냉장고에 재료를 한 번이라도 넣은 사용자 | **3** |
| 후기 | **1** |
| 레시피 즐겨찾기 | **4** |
| 레시피 좋아요 | **4** |
| 재료 즐겨찾기 | **2** |
| 사용자 재료 등록 | **1** |

사용자 43명의 아이디를 확인해보면 36개가 NULL(username 컬럼 도입 이전에 만들어진
계정)이고, 이름이 있는 7개는 `testuser01`, `test`, `gaptest0718`,
`verify_ui_v2_20260721`, `prod_verify_20260721`, `design_verify_20260726`,
`design2_verify_local` — **전부 개발·검증용 계정**이거나 개발자 본인 계정이다.

즉 **외부 실사용자는 0명이다.** 기능은 다 만들어졌지만 아무도 쓰지 않았다.
이것이 이 프로젝트의 가장 중요한 사실이며, 뒤의 모든 분석이 여기서 파생된다.

---

## 4. 그간 발생한 문제와 해결방안

28일 동안 겪은 문제 중 구조적으로 의미 있는 것만 추렸다.

### 4.1 psycopg2 파라미터 플레이스홀더 충돌

**증상.** 재료 검색이 프로덕션에서만 `IndexError: tuple index out of range`로
죽었다. 로컬 테스트(sqlite)는 전부 통과했다.

**원인.** `api/deps.py`의 `SqliteStyleCursor`가 sqlite 스타일 `?`를 psycopg2의
`%s`로 변환하는데, SQL 안에 있던 리터럴 `%`(LIKE `'%말린%'`)를 파라미터
플레이스홀더로 오인했다.

**해결.** LIKE 패턴을 문자열에 박지 않고 파라미터로 바인딩했다.

**교훈.** sqlite로 테스트하고 Postgres로 배포하는 구조는 **이런 종류의 버그를
테스트가 원천적으로 잡을 수 없다.** 지금도 그대로다(9절 참고).

### 4.2 FastAPI 라우트 순서에 의한 422

**증상.** `/recommendation/popular`이 422를 반환했다.

**원인.** 같은 라우터의 `/{recipe_id}`(int)가 먼저 등록돼 `popular`을 정수로
파싱하려 했다.

**해결.** 리터럴 경로를 파라미터 경로보다 먼저 등록하도록 `api/main.py`의 라우터
등록 순서를 조정했다. 이후 `/recipes/search`, `/demo`를 추가할 때도 같은 규칙을
적용했다.

### 4.3 Reflex는 자동 setter를 만들어주지 않는다

**증상.** 새로 만든 `DemoState`에서 `set_demo_ingredients` AttributeError.

**원인.** 기존 `State`가 쓰던 `set_field()`는 Reflex가 제공하는 게 아니라 이
프로젝트가 직접 정의한 헬퍼였다. 새 State 클래스에는 없었다.

**해결.** `DemoState`에도 같은 헬퍼를 정의했다.

### 4.4 OPENAI_API_KEY 미전달 — "Missing credentials"

**증상.** 냉장고 사진 인식이 프로덕션에서 SDK 원문 에러를 그대로 화면에 노출했다.

**원인.** Reflex Cloud 배포 시 `--envfile`을 붙이지 않아 키가 전달되지 않았다.

**해결.** 배포에 `--envfile`을 추가하고, 사용자에게 보이는 문구를 안내형으로
교체했으며 25초 타임아웃을 걸었다.

**부수 효과.** 이 과정에서 **에러 메시지를 사용자에게 그대로 노출하는 지점이
여기만이 아닐 수 있다**는 점이 드러났는데, 전수 점검은 하지 않았다.

### 4.5 로그인 LockExpiredError

**증상.** 프로덕션에서 로그인이 간헐적으로 실패했다.

**원인 진단이 이 프로젝트에서 가장 잘된 사례다.** 증상만 보면 "로그인 API가
느리다"로 보이지만, 실제로는 Reflex의 상태 락(기본 10초)이 문제였다. `login`
이벤트 하나가 프로필·냉장고·인기영상·제철재료·재료즐겨찾기를 **순차로** 불러오는데
각 요청 타임아웃이 10초라 최악의 경우 50초를 넘긴다. Render 무료 티어가 유휴
상태에서 깨어날 때 첫 요청이 실제로 그 한계를 넘겼다.

**해결.** `redis_lock_expiration=120000`(120초)으로 상향.

**남은 문제.** 이건 증상을 덮은 것이지 원인을 고친 게 아니다. 근본 해법은 5개
호출을 병렬화하거나 로그인 직후 필수 데이터만 불러오는 것이다(6절).

### 4.6 Supabase RLS 전면 비활성

**증상.** Supabase가 `rls_disabled_in_public` 보안 경고 메일을 보냈다.

**확인 결과.** public 스키마 15개 테이블 **전부** Row Level Security가 꺼져
있었다. Supabase의 anon 키는 클라이언트에 노출되는 것이 정상 설계인데, RLS가
없으면 그 키만으로 전체 테이블을 읽고 쓸 수 있다.

**해결.** 16개 테이블에 RLS를 활성화하고, 백엔드가 쓰는 연결 롤이 영향을 받지
않는지(users 41행, auth_tokens 12행 정상 조회) 확인했다.

**냉정한 평가.** 이건 외부에서 지적받고 나서야 고쳤다. **스스로 발견하지
못했다.** 보안 점검이 개발 프로세스에 들어 있지 않았다는 뜻이다.

### 4.7 Reflex Cloud 배포 파이프라인 장애 (가장 큰 손실)

**증상.** 2026-08-11부터 `reflex deploy`가 "Internal server error"로 실패하고,
배포 상태가 `Deploying`에 영구히 갇혔다.

**진단.** 16가지를 시도했다. 결정적 단서는 **pending이 하나도 없는 완전히 새 앱을
만들어 배포해도 같은 500이 났다**는 것이다. 즉 기존 pending은 원인이 아니라
결과였고, Reflex Cloud 서버측 장애가 확정됐다. CLI 소스를 뒤져 **배포 취소 API가
아예 존재하지 않는다**는 것도 확인했다.

**해결.** 하루를 더 기다리는 대신 호스팅을 옮겼다. 프론트는 Vercel 정적 호스팅,
Reflex 백엔드는 Render로 분리했다. 이전 전 유일한 미검증 항목이었던 "무료 티어
512MB에 Reflex 백엔드가 들어가는가"는 로컬 실측 **약 110MB**로 해소했다.

**이 사건에서 개인적으로 가장 값진 교훈은 진단 방법론 쪽이다.** 원인을 세 번
잘못 지목했는데(파이프가 프로세스를 끊었다 / 임시 파일이 사라졌다 / 인증이
만료됐다), 세 번 다 **명령 출력을 `tail`이나 파일 리다이렉트로 잘라 보면서 서버가
보낸 실제 실패 메시지를 놓친 탓**이었다. 출력을 자르지 않고 다시 돌리자 원인이
첫 줄에 그대로 있었다.

---

## 5. 디벨롭 필요한 부분

우선순위 순이다. 위쪽이 더 급하다.

### 5.1 테스트 커버리지의 절반이 비어 있다 — 최우선

67개 테스트가 통과한다는 말은 듣기 좋지만, **무엇을 테스트하지 않는지**가 더
중요하다.

18개 라우터 중 **9개에 테스트 파일이 없다.**

```
admin, favorite, ingredient_catalog, ingredient_submission,
nutrition, popular_videos, seasonal, shopping, user_recipe
```

이 중 `admin`은 레시피·재료 등록 승인 권한을 다루는 **가장 민감한 라우터**다.
엔드포인트 7개가 전부 테스트 없이 프로덕션에 있다. (무인증 접근이 401로 막히는
것은 오늘 직접 확인했지만, 그건 테스트가 아니라 일회성 확인이다.)

그리고 더 큰 구멍이 있다. **Reflex 프론트엔드 4,047줄에 테스트가 0개다.** 전체
코드의 33%가 커버리지 0%이며, 지금까지 프론트 검증은 전부 브라우저에서 사람이
클릭하는 방식이었다.

### 5.2 sqlite로 테스트하고 Postgres로 배포하는 구조

4.1에서 겪은 버그가 정확히 이 구조 때문에 새어나갔다. 테스트는 sqlite,
프로덕션은 Postgres이므로 **방언 차이에서 생기는 버그는 테스트가 절대 못 잡는다.**

해법은 CI에 Postgres 서비스 컨테이너를 띄우고 최소한 쿼리 계층 테스트만이라도
실제 Postgres로 돌리는 것이다. GitHub Actions에서 `services: postgres`로
어렵지 않게 된다.

### 5.3 추천 응답이 웜 상태에서도 2.6초다

오늘 실측한 값이다.

| 엔드포인트 | 콜드 | 웜 |
|---|---:|---:|
| `/recommendation/demo` | 36.3s | **2.6s** |
| `/recommendation/recipes/search` | — | 0.41s |

콜드 36초는 무료 티어 탓이라 어쩔 수 없다. 하지만 **웜 상태 2.6초는 코드 문제다.**
같은 서버의 검색 엔드포인트가 0.41초인 것과 비교하면 6배 이상 느리다. 추천 후보를
가져와 점수를 매기는 과정에 병목이 있다.

보류해둔 `get_candidate_recipes()`의 IN절→JOIN 리팩터가 여기에 직접 걸린다.
"데이터가 늘면 하자"고 미뤄뒀지만, 2.6초는 데이터가 안 늘어도 이미 사용자가
체감하는 수치다. **보류 근거를 재검토할 시점이다.**

### 5.4 로그인 순차 호출 5개

4.5의 근본 해법이다. 락 만료를 120초로 늘려 증상은 덮었지만, 로그인이 여전히
5개 API를 순차로 호출한다. `asyncio.gather`로 병렬화하거나, 로그인 직후에는
프로필만 불러오고 나머지는 각 탭 진입 시 지연 로딩하면 체감 속도가 크게 달라진다.

### 5.5 죽은 코드 3개

어디서도 import되지 않는 파일이다.

| 파일 | 내용 | 판단 |
|---|---|---|
| `recommendation_graph.py` | LangGraph 기반 LLM 추론 | 6절에서 별도 논의 |
| `notify_agent.py` | SMTP 알림 | 발송 경로가 없으면 삭제 |
| `clean_recipe_tags.py` | 태그 정제 일회성 스크립트 | `scripts/`로 이동하거나 삭제 |

---

## 6. 아쉬운 점

### 6.1 "AI 서비스"인데 핵심 추천에 AI가 없다

이게 가장 아픈 지점이다.

OpenAI를 실제로 호출하는 곳은 두 군데뿐이다. 후기 요약(`review_agent.py`)과
냉장고 사진 재료 인식(`naengjango_v2.py`)이다. **둘 다 부가 기능이다.**

정작 서비스의 심장인 추천 로직은 규칙 기반 점수 계산이다. 재료 겹침 수, 커버리지
비율, 영양군 매칭으로 순위를 매긴다. 나쁜 방식은 아니고 오히려 예측 가능하고
빠르다는 장점이 있지만, **LLM 추론을 담당하도록 만들어둔
`recommendation_graph.py`는 어디에도 연결되지 않은 채 죽어 있다.**

즉 "8개 에이전트 + LLM 추론"이라는 아키텍처 설명과 실제 동작 사이에 간극이 있다.
포트폴리오로 설명할 때 이 부분을 정직하게 말하지 않으면 심사에서 뚫린다.

### 6.2 디자인 개편을 두 번 했는데 첫 번째는 체감이 없었다

P0~P2 항목(터치 타겟 44px, Pretendard 폰트, CTA 색 분리, 빈 화면 처리, 스켈레톤
로딩)을 다 처리했는데 "별로 크게 달라진 게 없는 것 같다"는 반응이 나왔다.

그 지적이 옳았다. 실제로 화면 인상을 바꾼 건 그 다음에 한 세 가지 — 기본 액센트
색 교체(grass→green), 탭 아이콘 교체, 카드 이미지 중심 재배치 — 였다.

**교훈은 명확하다.** 접근성·일관성 개선(P0)은 반드시 필요하지만 **눈에 보이는
변화가 아니다.** 개편의 체감은 색·레이아웃·타이포 위계 같은 큰 덩어리에서 나온다.
작업 순서를 P0부터 잡은 것 자체가 잘못은 아니지만, "개편했다"는 기대치를 만들어
놓고 안 보이는 것부터 처리한 건 커뮤니케이션 실패였다.

### 6.3 사용자를 한 명도 만나지 않았다

28일 동안 84개 작업을 했고, 그중 사용자에게 물어보고 결정한 것은 **하나도 없다.**
추천 알고리즘을 9차까지 개정했는데 그 개정의 근거는 전부 개발자 본인의 판단이었다.

가장 아쉬운 건 이거다. **가족이나 친구 다섯 명에게 링크를 보내 일주일 써보게 하는
데는 하루도 안 걸린다.** 그 피드백 하나가 디자인 개편 2회분보다 방향 정확도를 더
높였을 것이다.

### 6.4 죽은 기능을 만드는 데 시간을 썼다

`shopping_agent`는 제휴 수수료가 연결되지 않는 단순 검색 URL 생성기다. 파일 자체에
"지금은 수익 연결이 안 되는 상태"라고 정직하게 적혀 있다. `notify_agent`는 발송
경로가 없다. `recommendation_graph`는 연결되지 않았다.

이 세 개에 들어간 시간을 테스트 커버리지나 실사용자 확보에 썼다면 프로젝트 상태가
지금보다 나았을 것이다.

---

## 7. 현실적인 벽

노력으로 넘을 수 없거나, 넘으려면 돈·시간·권한이 필요한 것들이다.

### 7.1 무료 티어 콜드스타트 36초 — 가장 높은 벽

Render 무료 티어는 15분 유휴 후 인스턴스를 내린다. 다시 깨어나는 데 오늘 실측
**36.3초**가 걸렸다.

이게 왜 치명적이냐면, **트래픽이 없는 서비스일수록 항상 콜드 상태**이기 때문이다.
지금처럼 사용자가 없으면 접속하는 사람은 100% 36초를 기다린다. 그리고 36초를
기다릴 사람은 없다. **사용자가 없어서 느리고, 느려서 사용자가 안 생기는 구조다.**

공모전 심사나 발표에서 링크를 열 때도 같은 문제가 생긴다. 미리 한 번 열어서
서버를 깨워두는 것 외에 무료 티어에서 할 수 있는 게 없다.

우회 수단은 있다. 외부 크론(UptimeRobot 등)으로 14분마다 핑을 보내 재우지 않는
방법인데, Render 약관상 회색지대이고 무료 인스턴스 시간(월 750시간)을 소진한다.
서비스 3개 중 2개가 Render이므로 계산이 빠듯하다.

정공법은 유료 전환이다. Render Starter가 서비스당 월 $7이니 두 개면 월 $14다.

### 7.2 공공 API 의존과 그 불안정성

외부 API를 호출하는 지점이 7곳이다.

| API | 사용처 | 리스크 |
|---|---|---|
| 식약처 식품영양성분DB | `ingredient_agent`, `ingredient_catalog_agent` | 간헐적 무응답 |
| 식약처 식품안전나라 | `safety_agent` | **간헐적 무응답 확인됨** |
| YouTube Data API | `popular_recipe_agent`, `youtube_agent` | **일일 할당량 제한** |
| KAMIS 가격정보 | `price_agent` | 품목 매칭 정확도 |

식약처 안전 API는 이미 무응답이 확인돼 503으로 우아하게 처리하도록 고쳤지만,
**그 말은 안전정보 기능이 외부 사정으로 언제든 동작하지 않을 수 있다**는 뜻이다.
"안전정보 자동확인"이 서비스의 차별점 중 하나인데 그 차별점이 내 통제 밖에 있다.

유튜브 할당량 제한은 실제로 벽으로 작용했다. 레시피 1,148개에 영상을 채우는 데
하루 50개씩 나눠 자동 실행해야 했고, 완료까지 여러 날이 걸렸다.

### 7.3 제휴 수익 모델의 진입 장벽

`shopping_agent`에 정리돼 있듯, 쿠팡파트너스는 URL에 파라미터를 붙이는 것으로
되지 않는다. 정식 승인 후 Deeplink API(HMAC 서명)를 거쳐야 수수료가 잡힌다.
네이버쇼핑은 보통 판매자별 개별 제휴다.

즉 **수익화는 코드 문제가 아니라 사업자 승인 문제**이고, 개인 프로젝트 단계에서
넘기 어렵다.

### 7.4 Reflex라는 선택 자체가 만든 제약

Reflex는 Python으로 React를 생성하는 프레임워크다. 브라우저와 websocket으로 상태를
주고받기 때문에 **상시 실행되는 Python 프로세스가 반드시 필요하다.**

이 한 가지 특성이 연쇄적으로 제약을 만들었다.

- Vercel·Netlify 같은 서버리스 정적 호스팅에 통째로 올릴 수 없다
- 그래서 프론트/백엔드를 분리해야 했고, 호스팅이 3개로 늘었다
- 상태 락(4.5), 자동 setter 부재(4.3) 같은 Reflex 고유 함정을 만났다
- 로컬 개발 중 websocket "Connection Error"가 반복돼 브라우저 검증이 자주 막혔다
- 프론트 테스트 도구 생태계가 사실상 없다(5.1의 커버리지 0%와 직결)

Reflex Cloud 장애까지 겪고 나면 "굳이 Reflex여야 했나"라는 질문이 자연스럽다.
다만 이건 **이미 결론이 난 사안**이다. React 전환을 대비해 현재 구조를
유지하기로 했고, 지금 와서 4,047줄을 다시 쓰는 비용은 정당화되지 않는다.

### 7.5 1인 개발의 검증 한계

코드를 쓴 사람이 테스트도 쓰고 검증도 한다. 4.6의 RLS 문제를 스스로 발견하지
못한 게 그 결과다. 리뷰어가 없으면 자기가 안 본 각도는 영원히 안 보인다.

---

## 8. 개선방안

우선순위와 소요를 함께 적었다. **위쪽 세 개가 압도적으로 중요하다.**

### 우선순위 1 — 실사용자 5명 확보 (반나절)

지금 이 프로젝트에 가장 부족한 것은 기능이 아니라 **사용 근거**다.

구체적으로 이렇게 한다. 가족·친구 5명에게 `/demo` 링크가 아니라 **가입이 필요한
본 서비스 링크**를 보내고, 일주일간 실제로 냉장고 재료를 넣어보게 한다. 첫 접속이
느릴 수 있다는 안내를 함께 보낸다. 일주일 뒤 DB를 조회해 재료 입력 수, 추천 호출
수, 후기 수를 본다.

여기서 나오는 데이터가 앞으로의 모든 우선순위를 정한다. 지금은 추측으로 정하고
있다.

### 우선순위 2 — 추천 웜 응답 2.6초 → 1초 이하 (1일)

`get_candidate_recipes()`의 IN절을 JOIN으로 바꾸고, 실행 계획을 확인해 필요한
인덱스를 추가한다. 보류 근거였던 "데이터가 안 늘었다"는 이제 유효하지 않다.
문제는 데이터 양이 아니라 쿼리 구조이고, 2.6초는 이미 체감되는 지연이다.

측정부터 한다. `EXPLAIN ANALYZE`로 후보 조회와 점수 계산 중 어디에 시간이 쓰이는지
갈라낸 다음 고친다. 짐작으로 손대지 않는다.

### 우선순위 3 — 콜드스타트 대응 결정 (30분 판단 + 실행)

세 선택지 중 하나를 **의식적으로 고른다.** 지금은 고르지 않은 채 방치돼 있다.

| 선택 | 비용 | 결과 |
|---|---|---|
| 그대로 둔다 | 0 | 첫 접속 36초. 실사용 확보는 사실상 포기 |
| 외부 크론 핑 | 0 | 대부분 웜 유지. 약관 회색지대, 무료 시간 소진 |
| Render Starter 전환 | 월 $14 | 콜드스타트 제거 |

공모전 제출이 목적이라면 첫째로도 충분하다(발표 전 미리 열어두면 된다). 실사용을
노린다면 둘째나 셋째가 필요하다. **목표에 따라 답이 갈리므로 목표를 먼저 정해야
한다.**

### 우선순위 4 — admin 라우터 테스트 (반나절)

권한 우회 시나리오 중심으로 짠다. 무인증 접근, 일반 사용자 토큰으로 접근, 승인
후 상태 전이 검증. 7개 엔드포인트에 각 2~3개면 충분하다.

### 우선순위 5 — CI에 Postgres 추가 (반나절)

GitHub Actions에 `services: postgres`를 추가하고, 최소한 쿼리를 직접 날리는
테스트만이라도 실제 Postgres로 돌린다. 4.1 같은 버그가 다시 새어나가는 걸 막는
유일한 방법이다.

### 우선순위 6 — 죽은 코드 정리 (1시간)

`notify_agent.py`, `clean_recipe_tags.py`를 삭제하거나 `scripts/`로 옮긴다.
`recommendation_graph.py`는 아래에서 따로 판단한다.

### 우선순위 7 — LLM 추론 연결 여부 결정 (판단)

`recommendation_graph.py`를 살릴지 지울지 정한다.

**살린다면** 전체 추천을 LLM으로 바꾸는 게 아니라, 규칙 기반으로 뽑은 상위 5개에
대해 **추천 이유 문장만 LLM으로 생성**하는 방식이 현실적이다. 응답 속도와 비용을
통제할 수 있고, "왜 이 메뉴인가"라는 설명 품질은 확실히 올라간다.

**지운다면** 포트폴리오 설명에서 "LLM 추론"을 빼고 "규칙 기반 다중 신호 랭킹"으로
정직하게 바꾼다. 이것도 충분히 설명 가치가 있다 — 9차에 걸쳐 개정한 랭킹 로직은
그 자체로 이야깃거리다.

**어느 쪽이든 지금 상태(만들어놓고 연결 안 함)보다는 낫다.**

---

## 9. 장단점

### 강점

**데이터 자산이 진짜다.** 식약처 영양성분 302,629행, 레시피 1,148개에 재료 태그
16,315개와 수량 정보 12,965건, 그리고 98%에 유튜브 영상까지 붙은 상태다. 이건
하루 이틀에 만들 수 없고, 경쟁 프로젝트가 쉽게 따라올 수 없는 부분이다.

**기능 폭이 넓다.** 53개 엔드포인트, 5탭 화면, 관리자 승인 큐까지 있다. 28일
1인 작업 결과물로는 이례적인 범위다.

**구조 분리가 깔끔하다.** 에이전트(도메인 로직) / 라우터(HTTP 계약) / 프론트가
분리돼 있어서 한 층을 바꿔도 다른 층이 흔들리지 않는다. 실제로 호스팅을 통째로
옮기면서 애플리케이션 코드는 `rxconfig.py` 한 줄만 고쳤다.

**주석 품질이 높다.** 코드에 "왜 이렇게 했는지"와 "무엇을 시도했다 실패했는지"가
날짜와 함께 적혀 있다. `shopping_agent.py`의 제휴 수수료 설명이 대표적이다.
나중에 이어받는 사람에게 이 정도 맥락이 남아 있는 개인 프로젝트는 드물다.

**장애 대응이 빨랐다.** Reflex Cloud가 죽었을 때 벤더 응답을 무한정 기다리지 않고
하루 만에 호스팅을 이전해 서비스를 살렸다. 이전 전에 메모리를 실측해 리스크를
확인하고 움직인 것도 적절했다.

### 약점

**사용자가 없다.** 43명 중 실사용자 0명. 모든 기능 판단이 검증되지 않은 가정 위에
서 있다.

**전체 코드의 33%가 테스트 0%다.** 프론트 4,047줄에 자동화 테스트가 없다. 라우터도
절반이 비어 있고, 그중엔 admin이 포함된다.

**테스트 환경과 운영 환경이 다르다.** sqlite로 검증하고 Postgres로 배포한다.
이미 한 번 버그가 새어나갔다.

**"AI 서비스"라는 정체성이 실제 구현과 어긋난다.** LLM은 후기 요약과 사진 인식이라는
곁가지에만 쓰이고, 핵심 추천은 규칙 기반이며 LLM 추론 모듈은 죽어 있다.

**성능이 좋지 않다.** 콜드 36초는 인프라 탓이지만 웜 2.6초는 코드 탓이다.

**무료 티어에 3중으로 묶여 있다.** Vercel(비상업용 Hobby), Render(512MB·유휴 중단),
Supabase(무료 한도). 어느 쪽이든 사용자가 늘면 즉시 벽에 닿는다.

---

## 10. 놓치고 있는 점

의도적으로 뺀 게 아니라 **논의 자체가 없었던** 항목들이다. 그래서 더 위험하다.

### 10.1 개인정보 처리방침이 없다

회원가입을 받고, 아이디·비밀번호·성별·연령대·건강목표·알레르기 정보를 저장한다.
**알레르기와 건강목표는 건강 관련 정보**다. 그런데 개인정보 처리방침도, 수집·이용
동의 절차도 없다.

지인 5명 테스트 수준이면 실질적 문제는 없다. 하지만 **공모전에 제출하는 순간
심사 항목이 될 수 있고**, 공개 서비스로 확장하면 법적 요구사항이다. 처리방침
페이지 하나 만드는 데 반나절이면 된다.

### 10.2 백업 전략이 없다

Supabase 무료 플랜의 자동 백업 정책을 확인한 기록이 없다. 302,629행짜리 영양DB와
1,148개 레시피는 다시 만들려면 상당한 시간이 든다.

`pg_dump`를 주기적으로 떠서 별도 저장소에 두는 것만으로도 대부분의 시나리오를
막을 수 있다. **지금 없다.**

### 10.3 에러를 관측할 방법이 없다

프로덕션에서 예외가 나면 어떻게 아는가. 지금은 **사용자가 말해주기 전까지 모른다.**
Sentry 무료 플랜이면 충분하고 연동에 한 시간이면 된다. 사용자가 0명일 때는 티가
안 나지만, 5명만 들어와도 즉시 필요해진다.

### 10.4 사용 로그가 없다

어떤 재료가 많이 입력되는지, 어떤 추천이 실제로 열람되는지, 어디서 이탈하는지
기록하지 않는다. **추천 알고리즘을 9차까지 개정했는데 그 개정이 좋아졌는지 나빠졌는지
측정할 수단이 없다.**

추천 호출과 레시피 상세 열람만 로그 테이블에 남겨도 다음 개정의 근거가 생긴다.

### 10.5 접근성을 실제로 측정하지 않았다

터치 타겟 44px과 Pretendard 폰트는 적용했지만, **명암비를 측정하거나 스크린리더로
읽어본 적이 없다.** 액센트 색을 grass에서 green으로 바꾼 것은 브랜드 판단이었고
대비 검증은 하지 않았다. Lighthouse 접근성 점수를 한 번 돌려보는 데 5분이면 된다.

### 10.6 라이선스와 데이터 출처 표기

화면 하단에 데이터 출처를 명시한 것은 잘한 일이다. 다만 **공공데이터포털 각
데이터셋의 이용약관을 개별 확인했는지**는 기록이 없다. 대부분 출처 표시 조건으로
자유 이용이 허용되지만, 상업적 이용 제한이 붙은 것이 섞여 있을 수 있다. 수익화를
검토한다면 반드시 확인해야 한다.

저장소 자체의 LICENSE 파일도 없다.

### 10.7 Reflex Cloud에 아직 구버전이 살아 있다

호스팅을 옮겼지만 기존 Reflex Cloud URL은 여전히 **구버전을 서빙 중**이다. 그
버전에는 `/demo`도 없고 로그인 락 수정도 반영돼 있지 않다.

지금은 무해하지만, 그 링크가 어딘가에 공유돼 있다면 사용자가 구버전을 만나게 된다.
어느 쪽이 정본인지 정리해야 한다.

### 10.8 Vercel Hobby 플랜은 비상업용이다

현재 프론트가 올라간 Vercel Hobby 플랜은 **상업적 이용이 약관상 허용되지 않는다.**
포트폴리오·공모전 용도라면 문제없지만, 수익화를 시도하는 순간 Pro 전환(월 $20)이
필요하다. 7.3의 제휴 수익 논의와 함께 봐야 할 항목이다.

---

## 11. 배포 관련 문제

### 11.1 28일 동안 배포 구조가 세 번 바뀌었다

| 시기 | 구성 |
|---|---|
| 초기 | FastAPI → Render, 프론트 → Reflex Cloud |
| 2026-08-11 | Reflex Cloud 장애로 프론트 배포 불가 |
| 2026-08-12 | 프론트 → Vercel, Reflex 백엔드 → Render |

**세 번 다 외부 요인이 아니라 초기 판단의 결과였다.** "왜 처음부터 Render로 한
번에 안 했나"라는 질문이 나왔던 이유다. 처음에 Reflex Cloud를 고른 것은 가장 쉬운
경로였기 때문이고, 그 선택이 벤더 장애 앞에서 취약했다.

### 11.2 배포 절차가 반자동이다

현재 절차는 이렇다.

```bash
API_URL="https://naengjango-hankki-v2-reflex.onrender.com" reflex export --frontend-only --no-zip
```

```bash
cd .web/build/client && vercel --prod
```

백엔드 두 개는 `master` 푸시 시 Render가 자동 배포한다. 즉 **백엔드는 자동,
프론트는 수동**이다.

이 비대칭이 위험하다. 백엔드 API를 바꾸고 푸시하면 백엔드만 갱신되고 프론트는
구버전으로 남는다. 프론트 export를 잊으면 조용히 어긋난다. 지금은 혼자 작업하니
기억으로 버티지만, GitHub Actions에 Vercel 배포를 붙이면 이 위험이 사라진다.

### 11.3 API_URL이 빌드 시점에 번들에 박힌다

이전 과정에서 실제로 걸린 문제다. `API_URL` 환경변수만 설정하고 export했더니
빌드 산출물에 `localhost:8000`이 그대로 들어갔다. Reflex가 이 값을 **빌드 시점의
config에서** 읽기 때문이다.

`rxconfig.py`에서 `api_url=os.getenv("API_URL", "http://localhost:8000")`으로
명시적으로 읽도록 고쳐 해결했지만, **구조적 취약점은 남아 있다.** 환경변수를 빼먹고
export하면 로컬 주소가 박힌 번들이 프로덕션에 올라가고, 빌드는 성공하므로
아무도 모른다.

배포 스크립트에 "번들에 localhost가 없는지" 확인하는 한 줄을 넣으면 막을 수 있다.

### 11.4 Reflex Cloud에 갇힌 배포 2건

`2ce7a213-…`와 `912b118e-…`가 `Deploying` 상태로 영구히 멈춰 있다. 지원팀(Sriman)은
"배포는 성공했지만 상태가 갇히는 문제를 고치는 중"이라고 답했고, 아직 해결되지
않았다.

더 이상 그 경로로 배포하지 않으므로 실질적 영향은 없지만, **취소 API가 존재하지
않아서 사용자가 스스로 정리할 방법이 없다**는 점은 기록해둘 가치가 있다. 호스팅
서비스를 고를 때 "실패한 배포를 되돌릴 수 있는가"를 체크리스트에 넣어야 한다는
교훈이다.

### 11.5 무료 티어 3중 의존

| 서비스 | 플랜 | 한계 |
|---|---|---|
| Vercel | Hobby | 비상업용 한정 |
| Render ×2 | Free | 512MB, 0.1 CPU, 15분 유휴 중단, 월 750시간 |
| Supabase | Free | 프로젝트 일시정지 정책 확인 필요 |

**Render 월 750시간은 서비스 2개로 나누면 계정당 여유가 크지 않다.** 두 서비스가
동시에 상시 가동되면 한 달에 약 1,440시간이 필요하므로, 유휴 중단이 오히려
무료 한도를 지켜주는 셈이다. 7.1에서 언급한 크론 핑으로 재우지 않으면 **월
중반에 한도를 소진할 수 있다.** 이 계산을 하지 않고 크론을 붙이면 서비스가
멈춘다.

### 11.6 롤백 수단이 사실상 없다

Render는 이전 배포로 롤백할 수 있지만, Vercel 프론트는 CLI로 직접 올리는 구조라
**빌드 산출물을 보관하지 않으면 이전 버전으로 되돌릴 수 없다.** git에서 이전
커밋을 체크아웃해 다시 export하면 되긴 하지만, 장애 상황에서 그 과정은 느리다.

Vercel 대시보드의 이전 배포로 Promote하는 기능이 있으니, 최소한 그 경로를
한 번 확인해두는 게 좋다.

---

## 12. 종합 판단

### 한 문장으로

**만드는 일은 거의 다 했고, 확인하는 일은 거의 안 했다.**

### 다시 시작한다면 다르게 할 세 가지

첫째, **기능 20개를 만들기 전에 사용자 5명을 먼저 확보한다.** 지금 84개 작업 중
상당수가 검증되지 않은 가정 위에서 이뤄졌다.

둘째, **테스트를 코드와 같은 속도로 쓴다.** 나중에 몰아서 쓰려니 절반이 비었고,
비어 있는 쪽이 하필 admin처럼 민감한 곳이다.

셋째, **호스팅을 고를 때 실패 경로를 먼저 본다.** "배포가 실패하면 어떻게
되돌리는가"를 확인했다면 Reflex Cloud에 하루를 잃지 않았을 것이다.

### 지금 당장 할 일 세 가지

1. **지인 5명에게 링크를 보낸다** (반나절) — 다른 모든 우선순위를 정하는 근거가 된다
2. **추천 쿼리를 `EXPLAIN ANALYZE`로 재보고 고친다** (1일) — 웜 2.6초는 이미 체감된다
3. **콜드스타트 대응을 셋 중 하나로 정한다** (30분) — 방치가 아니라 선택으로

### 목표별 판단

**공모전·포트폴리오가 목적이라면** 지금 상태로도 제출 가능하다. 다만 세 가지를
정직하게 정리해야 한다. LLM이 어디에 쓰였는지(핵심 추천에는 안 쓰임), 실사용자가
없다는 점, 콜드스타트 36초. 심사에서 물으면 나올 질문을 먼저 준비하는 쪽이 낫다.
"규칙 기반 9차 개정 랭킹"은 그 자체로 설명 가치가 충분하다.

**실사용 서비스가 목적이라면** 기능 추가를 멈추고 위의 세 가지부터 해야 한다.
지금 필요한 것은 85번째 기능이 아니라 **첫 번째 진짜 사용자**다.

---

*측정 근거: 코드 라인·커밋 수는 저장소 직접 집계, 테이블 행 수는 운영 Postgres
직접 조회, 응답 시간은 프로덕션 엔드포인트 `curl` 실측(2026-08-12), 테스트 67개는
`pytest` 실행 결과.*
